---
phase: quick/260506-sr7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/Cargo.toml
  - crates/core/src/decrypt.rs
  - crates/server/src/routes/decrypt.rs
  - crates/core/tests/round_trip.rs
  - crates/core/tests/fixtures/wallet.bed
  - crates/core/tests/fixtures/desc.txt
  - crates/core/tests/fixtures/xpub.txt
autonomous: true
requirements:
  - INTEROP-LIANA
quick_id: 260506-sr7

must_haves:
  truths:
    - "El crate `bitcoin-encrypted-backup` queda pinneado al tag publicado v0.0.2 (sha cd7ee382bf5ca0798d4f81697e2f9efb5e32fe40)."
    - "`cargo build --release` compila limpio sin enable de feature `base64` upstream."
    - "`cargo test --workspace` pasa en su totalidad (round-trip, cross-impl, qr-too-large, no_leak, etc.)."
    - "Un `.bed` real generado por Liana (`/workspace/claude/test.bed`) descifra a 200 OK con descriptor real vía `POST /api/decrypt` usando la xpub cosigner conocida."
    - "El binario release reiniciado responde HTTP 200 en `GET /` y atiende `/api/decrypt` con el nuevo crate."
    - "Comentarios y docs internas reflejan magic `BEB` y versión `0.0.2`; no quedan menciones obsoletas a `BIPXXX` ni `1.0.0` en código fuente o tests."
  artifacts:
    - path: "crates/core/Cargo.toml"
      provides: "Pin a tag v0.0.2 (sha cd7ee382...) y features sin `base64`"
      contains: "rev = \"cd7ee382bf5ca0798d4f81697e2f9efb5e32fe40\""
    - path: "crates/core/src/decrypt.rs"
      provides: "Wrapper de descifrado con doc actualizado al contrato 0.0.2"
      contains: "BEB"
    - path: "crates/core/tests/round_trip.rs"
      provides: "Tests de round-trip y cross-impl regenerados con magic BEB"
      contains: "b\"BEB\""
    - path: "crates/core/tests/fixtures/wallet.bed"
      provides: "Fixture cifrado con la crate v0.0.2 in-tree (regenerado)"
      min_bytes: 64
  key_links:
    - from: "crates/core/Cargo.toml"
      to: "github.com/pythcoiner/encrypted_backup@cd7ee382"
      via: "git rev pin (tag v0.0.2)"
      pattern: "cd7ee382bf5ca0798d4f81697e2f9efb5e32fe40"
    - from: "crates/server/src/routes/decrypt.rs"
      to: "/workspace/claude/test.bed"
      via: "POST /api/decrypt multipart (curl smoke test)"
      pattern: "decrypt_payload"
    - from: "crates/core/tests/round_trip.rs"
      to: "crates/core/tests/fixtures/wallet.bed"
      via: "include_bytes! cross-impl test"
      pattern: "fixtures/wallet.bed"
---

<objective>
Migrar la dependencia `bitcoin-encrypted-backup` de master HEAD (autodeclarado "1.0.0", nunca publicado) al tag publicado `v0.0.2` (sha `cd7ee382bf5ca0798d4f81697e2f9efb5e32fe40`) para conseguir interoperabilidad cripto-compatible con Liana.

Purpose: Liana en producción usa `bitcoin-encrypted-backup = "=0.0.2"`. Nuestro pin actual (HEAD master) cambia el algoritmo (AES-256-GCM → ChaCha20-Poly1305), el magic (`BEB` → `BIPXXX` placeholder) y derivación de claves (x-only + BIP340), por lo que un `.bed` real de Liana NO se puede descifrar con nuestra build. La migración a v0.0.2 cierra la brecha y restaura interop end-to-end.

Output: `Cargo.toml` actualizado, fixtures de tests regenerados con la crate v0.0.2 in-tree, comentarios obsoletos saneados, build release verde, tests verdes, smoke test acid contra `test.bed` devolviendo 200 OK.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@crates/core/Cargo.toml
@crates/core/src/decrypt.rs
@crates/core/src/encrypt.rs
@crates/core/src/error.rs
@crates/core/src/lib.rs
@crates/core/src/armored.rs
@crates/core/tests/round_trip.rs
@crates/server/src/routes/decrypt.rs

<interfaces>
<!-- API que la crate `bitcoin-encrypted-backup@v0.0.2` expone y que ya usamos. Verificada
     por auditoría manual del Cargo.toml y src/lib.rs del tag cd7ee382. NO cambia entre HEAD
     y v0.0.2 en lo que respecta a símbolos consumidos por bed-core/bed-server. -->

Re-exports utilizados (todos presentes en v0.0.2):
- `EncryptedBackup::new()`
- `EncryptedBackup::set_payload(&Descriptor<DescriptorPublicKey>)`
- `EncryptedBackup::set_encrypted_payload(&[u8])` — en v0.0.2 acepta SOLO binario;
  no autodetecta base64. Como nuestro `armored::decode_armored` ya devuelve binario
  el contrato real no cambia; solo hay que actualizar comentarios.
- `EncryptedBackup::set_keys(Vec<secp256k1::PublicKey>)`
- `EncryptedBackup::encrypt() -> Vec<u8>` (binario con magic `BEB`)
- `EncryptedBackup::decrypt() -> Decrypted`
- `Decrypted::Descriptor(d)` con `d: Descriptor<DescriptorPublicKey>`
- `descriptor::dpk_to_pk(&DescriptorPublicKey) -> secp256k1::PublicKey`
- Re-export `bitcoin_encrypted_backup::miniscript`
- `Error` con variantes `WrongKey`, `NoKey`, `DescriptorHasNoKeys`, `Descriptor`, `Utf8`

Diferencias respecto a HEAD:
- Feature `base64` del crate **NO existe** en v0.0.2 (añadida en commit 55054b4 posterior).
  Nuestro `armored.rs` usa la crate `base64` directa del workspace, así que retirar la
  feature del array de la dep no afecta funcionalidad.
- Magic bytes en el binario `.bed`: v0.0.2 = `BEB`. HEAD = `BIPXXX`. Tests y comentarios
  actualizan a `BEB`.
- Algoritmo simétrico: v0.0.2 = AES-256-GCM. Suficientemente opaco a nivel de wrapper,
  no requiere cambios en `encrypt.rs`/`decrypt.rs`.
</interfaces>

<task_context>
- Causa raíz verificada por curl + lectura del git log upstream.
- `test.bed` de Liana imposible de descifrar con HEAD (magic mismatch + algoritmo distinto).
- Comportamiento esperado tras migración: el fixture legacy generado por la CLI `beb` HEAD
  (`/tmp/bed-test/wallet.bed`) deja de descifrar — es correcto. Hay que regenerar el fixture
  in-tree (`crates/core/tests/fixtures/wallet.bed`) cifrando con la crate v0.0.2 ya pinneada.
- Email noreply para git: 55397917+4rkad@users.noreply.github.com.
- Idioma: castellano (no argentino).
</task_context>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin Cargo.toml a v0.0.2 (sha cd7ee382) y retirar feature `base64` upstream</name>
  <files>crates/core/Cargo.toml</files>
  <action>
    Editar `crates/core/Cargo.toml` sección `[dependencies.bitcoin-encrypted-backup]`:

    1. Cambiar `rev = "17b69b71cd1e005f80f5e81147795df0d11db027"` → `rev = "cd7ee382bf5ca0798d4f81697e2f9efb5e32fe40"`.
    2. Reemplazar el comentario actual ("Pinned to HEAD on master as of 2026-05-05 ...")
       por explicación clara: este sha corresponde al tag publicado `v0.0.2`, que es la
       versión que Liana en producción usa (`bitcoin-encrypted-backup = "=0.0.2"`). Migrar
       a HEAD rompe interop porque cambia magic (`BEB`→placeholder), algoritmo
       (AES-256-GCM→ChaCha20-Poly1305) y derivación de claves (x-only + BIP340).
    3. Modificar el array `features = ["miniscript_12_3_5", "rand", "base64"]`:
       quitar `"base64"` (esa feature **no existe** en v0.0.2; fue añadida después en
       commit 55054b4). Resultado: `features = ["miniscript_12_3_5", "rand"]`.
    4. Mantener intacta la línea `# DO NOT enable: "devices" ..., "cli" ..., "tokio"`.

    NO tocar `default-features = false`, `[lints]`, ni el resto de dependencias del
    `[dependencies]` superior (zeroize, thiserror, base64 del workspace, qrcode, image).
  </action>
  <verify>
    <automated>cd /workspace/descriptor-cifrado && cargo build --release -p bed-core 2>&1 | tail -20 && grep -q 'rev = "cd7ee382bf5ca0798d4f81697e2f9efb5e32fe40"' crates/core/Cargo.toml && ! grep -q '"base64"' crates/core/Cargo.toml</automated>
  </verify>
  <done>
    `crates/core/Cargo.toml` apunta al sha `cd7ee382...`, ya no enable la feature `base64`
    de la crate `bitcoin-encrypted-backup`, y `cargo build --release -p bed-core` compila
    sin errores. `Cargo.lock` actualizado al nuevo sha.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sanear comentarios obsoletos (`BIPXXX` → `BEB`) en código y server</name>
  <files>crates/core/src/decrypt.rs, crates/server/src/routes/decrypt.rs</files>
  <action>
    Reformular comentarios para que reflejen el contrato real de v0.0.2.

    **`crates/core/src/decrypt.rs`** (líneas 3-5 actuales):
    Reemplazar el bloque doc:
      ```
      //! The crate's `set_encrypted_payload` auto-detects binary "BIPXXX" magic vs
      //! base64 — but does NOT strip PEM-style headers. The server is responsible
      //! for stripping headers via `armored::decode_armored` BEFORE calling this.
      ```
    por:
      ```
      //! En `bitcoin-encrypted-backup` v0.0.2 (tag publicado, compatible con Liana),
      //! `set_encrypted_payload` espera el blob binario con magic `BEB`. NO hace
      //! autodetección de base64 ni strip de PEM. El server decodifica armored vía
      //! `armored::decode_armored` ANTES de llamar a esta función; este wrapper solo
      //! recibe binario crudo.
      ```

    **`crates/server/src/routes/decrypt.rs`** (líneas ~55-57):
    Reemplazar el comentario:
      ```
      // Auto-detect armored: if bytes start with "-----BEGIN", strip PEM headers
      // via decode_armored. Otherwise pass raw bytes to crate (which auto-detects
      // binary "BIPXXX" magic vs raw base64).
      ```
    por:
      ```
      // Auto-detect armored: si los bytes empiezan con "-----BEGIN", quitar headers
      // PEM vía decode_armored. En caso contrario, pasar binario crudo a la crate
      // (v0.0.2 espera magic `BEB` y no acepta base64 raw — el frontend siempre nos
      // manda binario o armored, nunca base64 suelto).
      ```

    NO tocar lógica ejecutable, solo comentarios. NO modificar la rama `if bed_bytes.starts_with(b"-----BEGIN")`.

    Buscar también cualquier otra mención literal de "BIPXXX" en `crates/**/src/*.rs`:
      `grep -rn 'BIPXXX' crates/`
    Si aparece en otro `.rs` (que no sea test), corregir a `BEB`.
  </action>
  <verify>
    <automated>cd /workspace/descriptor-cifrado && ! grep -rn 'BIPXXX' crates/core/src/ crates/server/src/ && cargo build --release 2>&1 | tail -5</automated>
  </verify>
  <done>
    Ningún archivo bajo `crates/*/src/` contiene la cadena `BIPXXX`. Build release sigue
    verde. Los comentarios reflejan el contrato v0.0.2 y mencionan magic `BEB`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Regenerar fixture `wallet.bed` in-tree y actualizar tests al magic `BEB`</name>
  <files>crates/core/tests/round_trip.rs, crates/core/tests/fixtures/wallet.bed</files>
  <action>
    El fixture `crates/core/tests/fixtures/wallet.bed` fue generado con la CLI `beb` de
    HEAD (magic `BIPXXX`). Tras pinnear v0.0.2 dejará de descifrar (magic mismatch +
    algoritmo distinto). Hay que regenerarlo USANDO LA PROPIA CRATE v0.0.2 que ya está
    pinneada en `Cargo.toml`. Pasos:

    1. Crear un binario auxiliar one-shot (no commiteado al repo final) en
       `crates/core/examples/regen_fixture.rs` que:
        - Lee `crates/core/tests/fixtures/desc.txt` (cleartext del descriptor multisig).
        - Llama `bed_core::encrypt_descriptor(&mut Zeroizing::new(desc))`.
        - Escribe `out.bed_bytes` a `crates/core/tests/fixtures/wallet.bed`.
       Ejecutar: `cargo run --release --example regen_fixture -p bed-core`.
       Verificar: `head -c 3 crates/core/tests/fixtures/wallet.bed` imprime `BEB`.
       Borrar el example tras regenerar (no es código de producción).

       Alternativa equivalente si el binario one-shot resulta engorroso: añadir un
       `#[ignore]`-marked test `regenerate_fixture` que escribe el fixture, correrlo con
       `cargo test -p bed-core regenerate_fixture -- --ignored`, y luego borrarlo.

    2. Editar `crates/core/tests/round_trip.rs` líneas 52-56:
        - Reemplazar el comentario `// Sanity: bed_bytes start with "BIPXXX" magic ...`
          por `// Sanity: bed_bytes empiezan con magic "BEB" (bitcoin-encrypted-backup v0.0.2).`
        - Cambiar `out.bed_bytes.starts_with(b"BIPXXX")` → `out.bed_bytes.starts_with(b"BEB")`.
        - Cambiar el mensaje de assert `"bed must start with BIPXXX magic"` →
          `"bed debe empezar con magic BEB"`.

    3. NO tocar el resto de tests (`decrypt_with_wrong_xpub_fails`,
       `cross_implementation_decrypt_with_reference_bed`, `qr_too_large_returns_error`):
       el cross-impl test seguirá funcionando porque `wallet.bed` se ha regenerado con
       la misma crate v0.0.2 contra la que se compila el test, y `desc.txt`/`xpub.txt`
       no cambian.

    4. NO regenerar `desc.txt` ni `xpub.txt` — el descriptor cleartext es estable y la
       xpub cosigner es la misma. Solo el blob binario cambia de algoritmo.
  </action>
  <verify>
    <automated>cd /workspace/descriptor-cifrado && head -c 3 crates/core/tests/fixtures/wallet.bed | xxd | grep -q '4245 42' && cargo test --workspace --release 2>&1 | tail -30 | grep -E 'test result: ok|FAILED'</automated>
  </verify>
  <done>
    Fixture `crates/core/tests/fixtures/wallet.bed` regenerado, sus 3 primeros bytes son
    `BEB` (0x42 0x45 0x42). Test `round_trip_fixture` y `cross_implementation_decrypt_with_reference_bed`
    pasan. `cargo test --workspace --release` finaliza con `test result: ok` en todos
    los binarios de test del workspace.
  </done>
</task>

<task type="auto">
  <name>Task 4: Smoke test acid de interop con Liana (`test.bed`) contra binario release reiniciado</name>
  <files>(no source files; verificación end-to-end)</files>
  <action>
    Con `Cargo.toml` ya en v0.0.2 y tests verdes, validar interop real con un `.bed`
    producido por Liana en producción.

    1. Build release final del server:
       ```
       cd /workspace/descriptor-cifrado && cargo build --release -p bed-server
       ```

    2. Reiniciar el binario release matando la instancia actual:
       ```
       pkill -f 'target/release/bed-server' || true
       sleep 1
       BED_DATA_DIR=/tmp/bed-uat-data RUST_LOG=info \
         /workspace/descriptor-cifrado/target/release/bed-server \
         > /tmp/bed-server.log 2>&1 &
       sleep 2
       ```

    3. Verificar liveness:
       ```
       curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8080/
       ```
       Debe imprimir `200`. Si imprime otro código, leer `/tmp/bed-server.log` y
       diagnosticar antes de continuar.

    4. Smoke test acid contra `.bed` real de Liana:
       ```
       curl -s -o /tmp/bed-decrypt-response.json -w "%{http_code}\n" \
         -X POST http://127.0.0.1:8080/api/decrypt \
         -F "bed=@/workspace/claude/test.bed" \
         -F "xpub=xpub6PLACEHOLDER1xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
       ```
       - Código HTTP **DEBE** ser `200`.
       - `cat /tmp/bed-decrypt-response.json` debe contener un JSON con un descriptor
         real (empieza por `wsh(`, `tr(`, `sh(`, `wpkh(`, etc.; NO un mensaje de error).

    5. Si HTTP != 200 o el JSON no contiene descriptor: NO marcar el plan como
       completo. Diagnosticar (probable mismatch de xpub cosigner o que `test.bed`
       venga de una versión distinta de Liana). Reportar al usuario antes de cerrar.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" -X POST http://127.0.0.1:8080/api/decrypt -F "bed=@/workspace/claude/test.bed" -F "xpub=xpub6PLACEHOLDER1xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx" | grep -q '^200$'</automated>
  </verify>
  <done>
    Binario release reiniciado responde 200 en `/`. `POST /api/decrypt` con `test.bed` +
    xpub cosigner devuelve HTTP 200 con descriptor real en el JSON. Interop con Liana
    en producción confirmada end-to-end.
  </done>
</task>

</tasks>

<verification>
1. `cargo build --release` (workspace completo) — verde.
2. `cargo test --workspace --release` — todos los tests pasan, incluyendo `round_trip_fixture`,
   `cross_implementation_decrypt_with_reference_bed`, `decrypt_with_wrong_xpub_fails`,
   `qr_too_large_returns_error` y los tests de history (`no_leak`, etc.).
3. `! grep -rn 'BIPXXX' crates/*/src/ crates/*/tests/` — sin residuos de magic obsoleto en código fuente.
4. `grep -q 'cd7ee382bf5ca0798d4f81697e2f9efb5e32fe40' crates/core/Cargo.toml` — pin correcto.
5. `! grep -q '"base64"' crates/core/Cargo.toml` — feature retirada.
6. `head -c 3 crates/core/tests/fixtures/wallet.bed` muestra bytes `BEB`.
7. Smoke test acid contra `test.bed` devuelve HTTP 200 con descriptor real.
8. Binario release reiniciado responde 200 en `/` con el nuevo crate.
</verification>

<success_criteria>
- [ ] `crates/core/Cargo.toml` pinnea sha `cd7ee382bf5ca0798d4f81697e2f9efb5e32fe40` (v0.0.2).
- [ ] Feature `base64` retirada del array `features` de la dep `bitcoin-encrypted-backup`.
- [ ] Comentario en `Cargo.toml` explica la razón del pin (interop con Liana, no migrar a HEAD).
- [ ] Comentarios en `crates/core/src/decrypt.rs` y `crates/server/src/routes/decrypt.rs`
      reflejan el contrato v0.0.2 (magic `BEB`, sin autodetección de base64).
- [ ] Fixture `crates/core/tests/fixtures/wallet.bed` regenerado con la crate v0.0.2 in-tree;
      sus 3 primeros bytes son `BEB`.
- [ ] Test `round_trip_fixture` asserta `b"BEB"` en lugar de `b"BIPXXX"`.
- [ ] `cargo build --release` y `cargo test --workspace --release` finalizan sin errores.
- [ ] No hay menciones literales de `BIPXXX` en `crates/*/src/` ni en `crates/*/tests/`.
- [ ] Binario release reiniciado responde HTTP 200 en `GET /`.
- [ ] `POST /api/decrypt` con `/workspace/claude/test.bed` + xpub cosigner conocida
      devuelve HTTP 200 y un JSON con descriptor real (interop confirmada con Liana).
</success_criteria>

<output>
Tras completar las 4 tareas, crear
`.planning/quick/260506-sr7-migrar-bitcoin-encrypted-backup-a-v0-0-2/260506-sr7-SUMMARY.md`
documentando:
- Causa raíz (pin a HEAD vs tag publicado 0.0.2 que usa Liana).
- Cambios concretos en `Cargo.toml`, `decrypt.rs`, comentario del server, fixtures de tests.
- Resultado del smoke test acid contra `test.bed`.
- Nota explícita: "fixture legacy `/tmp/bed-test/wallet.bed` (CLI `beb` HEAD) ahora
  deprecado; regenerar si se quiere seguir usando como referencia externa, o
  reemplazar por uno cifrado con la CLI `beb` v0.0.2".
- Commit hash final.

Commit con mensaje: `fix(crypto): pin bitcoin-encrypted-backup a v0.0.2 para interop con Liana`
y email noreply `55397917+4rkad@users.noreply.github.com`.
</output>
