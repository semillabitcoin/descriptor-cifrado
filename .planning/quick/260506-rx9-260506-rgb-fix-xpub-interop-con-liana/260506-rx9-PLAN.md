---
phase: 260506-rx9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/xpub.js
  - frontend/src/components/TabDescifrar.svelte
autonomous: false
requirements:
  - XPUB-INTEROP-LIANA
must_haves:
  truths:
    - "Pegar una xpub descriptor-style de Liana en el campo xpub del tab Descifrar habilita el botón Descifrar (validación cliente acepta el formato)."
    - "Al pulsar Descifrar con xpub descriptor-style + .bed válido, el frontend envía al backend la xpub bare (sin prefix [fp/path] ni sufijo /<0;1>/* o /*) y obtiene HTTP 200 con el descriptor descifrado."
    - "Pegar una xpub bare clásica sigue funcionando exactamente igual que antes (no regresión)."
    - "Pegar texto que no es xpub (ni bare ni descriptor-style) sigue dejando el botón Descifrar disabled."
  artifacts:
    - path: "frontend/src/lib/xpub.js"
      provides: "validateXpub acepta descriptor-style; export normalizeXpub strippea prefix/suffix"
      contains: "export function normalizeXpub"
    - path: "frontend/src/components/TabDescifrar.svelte"
      provides: "Construye body POST /api/decrypt usando xpub normalizada (bare)"
      contains: "normalizeXpub"
  key_links:
    - from: "frontend/src/components/TabDescifrar.svelte"
      to: "frontend/src/lib/xpub.js"
      via: "import { validateXpub, normalizeXpub }"
      pattern: "normalizeXpub\\(xpubText"
    - from: "TabDescifrar.handleDescifrar"
      to: "POST /api/decrypt body field 'xpub'"
      via: "formData.append('xpub', normalizeXpub(xpubText.trim()))"
      pattern: "formData\\.append\\(['\"]xpub['\"], *normalizeXpub"
---

<objective>
Aceptar en el frontend xpubs descriptor-style que exporta Liana wallet (formato `[fingerprint/derivation]xpub.../<0;1>/*` o `[fp/path]xpub.../*`), normalizándolas a xpub bare antes de enviarlas al backend.

Purpose: El caso de uso primario "recover from Liana .bed" está roto porque la regex cliente rechaza el formato descriptor-style antes de que llegue al backend (que de hecho ya descifra correctamente cuando recibe la xpub bare). Confirmado por curl: bare → 200, descriptor-style → 422 DESCRIPTOR_PARSE.

Output:
- `frontend/src/lib/xpub.js` con `normalizeXpub` exportada y `validateXpub` que normaliza antes de testar.
- `frontend/src/components/TabDescifrar.svelte` que llama `normalizeXpub` antes del POST.
- Smoke test round-trip ejecutado en checkpoint humano contra `/tmp/bed-test/wallet.bed` con `key1.txt` (descriptor-style).

Backend NO se toca (Phase 1 stable contract — espera xpub bare).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@frontend/src/lib/xpub.js
@frontend/src/components/TabDescifrar.svelte

<interfaces>
<!-- Estado actual de xpub.js (a modificar): -->

```js
// frontend/src/lib/xpub.js (current)
export const XPUB_REGEX = /^([xyzt]pub|tpub)[A-Za-z0-9]{100,}$/;

export function validateXpub(text) {
  if (typeof text !== 'string') return false;
  const trimmed = text.trim();
  return XPUB_REGEX.test(trimmed);
}
```

<!-- Punto de uso en TabDescifrar.svelte (current, líneas 88-101): -->

```js
const formData = new FormData();
if (bedFile) {
  formData.append('bed', bedFile, bedFile.name);
} else {
  const armoredBlob = new Blob([armoredText.trim()], { type: 'text/plain' });
  formData.append('bed', armoredBlob, 'armored.txt');
}
formData.append('xpub', xpubText.trim());  // ← aquí se envía xpub raw

const resp = await postMultipart('/api/decrypt', formData);
```

<!-- Validación derivada en TabDescifrar.svelte (línea 25): -->

```js
let xpubReady = $derived(validateXpub(xpubText));
```

<!-- Formatos que normalizeXpub debe aceptar y a qué deben reducirse: -->

Input descriptor-style típico de Liana:
  [68a9ec24/48h/0h/0h/2h]xpub6PLACEHOLDER2xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx/<0;1>/*
  → bare:
  xpub6PLACEHOLDER2xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

Variantes a soportar:
  - Con o sin prefix `[...]` (cualquier contenido entre corchetes balanceados al inicio).
  - Con o sin sufijo de derivación: `/<0;1>/*`, `/*`, `/0/*`, `/1/*`, multipath `/<m;n>/*`.
  - Si el input ya es bare (xpub|ypub|zpub|tpub + base58), devolver tal cual.
  - Whitespace al inicio/fin: trim antes de procesar.

Fixtures de validación (en /tmp/bed-test/):
  - key1.txt, key2.txt, key3.txt → xpubs descriptor-style de Liana (cualquiera sirve)
  - wallet.bed → binario .bed para round-trip
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Añadir normalizeXpub y actualizar validateXpub en xpub.js</name>
  <files>frontend/src/lib/xpub.js</files>
  <behavior>
    - normalizeXpub(text) recibe string; trimea; strippea prefix `[...]` (regex `^\[[^\]]*\]`) si presente; strippea sufijo de derivación tras la xpub (regex `/(<\d+;\d+>|\d+|\*)(/\*)?$` o más conservador: `/.*$` después del último carácter base58 válido). Devuelve string (xpub bare o input limpio si no matchea formato esperado).
    - Si el input ya es bare (sin `[` ni `/` post-xpub), devolverlo trimeado.
    - validateXpub(text) llama normalizeXpub primero y testa XPUB_REGEX contra el resultado.
    - Casos edge:
      * Input vacío o no-string → validateXpub false (sin throw).
      * Input con solo prefix `[fp/path]` sin xpub → validateXpub false.
      * Input bare clásico → sin cambio en comportamiento (regresión cero).
  </behavior>
  <action>
    Reescribir `frontend/src/lib/xpub.js` para:

    1. Mantener `XPUB_REGEX` igual (sigue siendo la fuente de verdad del formato bare).
    2. Añadir `export function normalizeXpub(text)`:
       - Si `typeof text !== 'string'`, devolver `''`.
       - `let s = text.trim()`.
       - Strip prefix descriptor: `s = s.replace(/^\[[^\]]*\]/, '')`.
       - Strip sufijo de derivación tras la xpub: usar `s = s.replace(/\/[^\s]*$/, '')` para eliminar todo desde el primer `/` hasta el final (incluye `/<0;1>/*`, `/*`, `/0/*`, `/1/*`, multipath, etc.). Nota: una xpub bare base58 no contiene `/`, así que esto es seguro.
       - Devolver `s`.
    3. Modificar `validateXpub(text)`:
       - Si `typeof text !== 'string'`, devolver `false`.
       - `const normalized = normalizeXpub(text);`
       - Devolver `XPUB_REGEX.test(normalized)`.

    Mantener el comentario de cabecera; añadir comentario sobre interop Liana (descriptor-style) en español castellano.

    Decisión D-XPUB-NORM (locked): el strip de derivación usa el approach permisivo `/.*$` porque la xpub base58 nunca contiene `/`. Esto cubre todas las variantes de derivación BIP-380 y multipath sin enumerar regex frágiles.
  </action>
  <verify>
    <automated>cd /workspace/descriptor-cifrado/frontend && node -e "import('./src/lib/xpub.js').then(m => { const cases = [['xpub6PLACEHOLDER2xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx', true, 'bare'], ['[68a9ec24/48h/0h/0h/2h]xpub6PLACEHOLDER2xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx/<0;1>/*', true, 'descriptor'], ['  xpub6PLACEHOLDER2xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx  ', true, 'whitespace'], ['no es una xpub', false, 'garbage'], ['', false, 'empty'], ['[fp]', false, 'only-prefix']]; let fail = 0; for (const [input, expected, label] of cases) { const got = m.validateXpub(input); if (got !== expected) { console.error('FAIL', label, 'input=', JSON.stringify(input.slice(0,50)), 'expected', expected, 'got', got); fail++; } else { console.log('OK', label); } } const norm = m.normalizeXpub('[68a9ec24/48h/0h/0h/2h]xpub6PLACEHOLDER2xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx/<0;1>/*'); const expected_bare = 'xpub6PLACEHOLDER2xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx'; if (norm !== expected_bare) { console.error('FAIL normalize roundtrip: got', norm); fail++; } else { console.log('OK normalize-roundtrip'); } process.exit(fail > 0 ? 1 : 0); });"</automated>
  </verify>
  <done>
    - `xpub.js` exporta `normalizeXpub` y `XPUB_REGEX`; `validateXpub` normaliza antes de testar.
    - El comando inline node arriba imprime "OK" para los 6 casos + "OK normalize-roundtrip" y exit 0.
    - Aceptar xpub bare clásica (regresión cero).
    - Aceptar descriptor-style con prefix + sufijo `/<0;1>/*`.
    - Rechazar input vacío, garbage, solo-prefix.
    - normalizeXpub devuelve exactamente la xpub bare cuando recibe descriptor-style.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Llamar normalizeXpub en TabDescifrar antes del POST</name>
  <files>frontend/src/components/TabDescifrar.svelte</files>
  <action>
    Modificar `frontend/src/components/TabDescifrar.svelte`:

    1. En el import de la línea 3, añadir `normalizeXpub`:
       ```js
       import { validateXpub, normalizeXpub } from '../lib/xpub.js';
       ```

    2. En `handleDescifrar` (línea 96), reemplazar:
       ```js
       formData.append('xpub', xpubText.trim());
       ```
       por:
       ```js
       formData.append('xpub', normalizeXpub(xpubText));
       ```
       (normalizeXpub ya hace trim internamente y strippea prefix/sufijo).

    3. NO tocar `xpubReady = $derived(validateXpub(xpubText))` (línea 25): validateXpub ya normaliza internamente desde Task 1, así que el botón se habilita correctamente con descriptor-style sin más cambios.

    4. NO tocar la limpieza `xpubText = ''` post-descifrado (línea 101).

    Mantener todo el resto del componente sin cambios. No introducir nuevas variables de estado.
  </action>
  <verify>
    <automated>cd /workspace/descriptor-cifrado/frontend && grep -n "normalizeXpub" src/components/TabDescifrar.svelte && grep -c "formData.append('xpub', normalizeXpub" src/components/TabDescifrar.svelte | grep -q "^1$" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Import de `normalizeXpub` presente en TabDescifrar.svelte.
    - Línea `formData.append('xpub', normalizeXpub(xpubText))` aparece exactamente 1 vez.
    - `npm run build` completa sin errores ni warnings nuevos.
    - Sin cambios en `xpubReady` derivation ni en la limpieza post-descifrado.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Smoke test round-trip con fixture Liana</name>
  <what-built>
    - `normalizeXpub` strippea prefix `[fp/path]` y sufijo `/<0;1>/*` o `/*` para devolver xpub bare.
    - `validateXpub` ahora acepta formato descriptor-style.
    - `TabDescifrar.svelte` envía xpub normalizada (bare) al backend.
  </what-built>
  <how-to-verify>
    1. Asegurar que el backend está corriendo (cargo run o el binario actual de Phase 1) escuchando en `http://127.0.0.1:8080` (o el puerto que use el dev server). Si no lo está, arrancarlo en otra terminal.

    2. Levantar el frontend en modo dev:
       ```
       cd /workspace/descriptor-cifrado/frontend && npm run dev
       ```
       Abrir la URL que imprima Vite (típicamente `http://localhost:5173`).

    3. Ir al tab "Descifrar".

    4. Cargar el archivo `.bed`:
       - Drag-and-drop `/tmp/bed-test/wallet.bed` en la dropzone.
       - Verificar que aparece "Archivo cargado: wallet.bed".

    5. Pegar xpub descriptor-style en el campo xpub:
       - Copiar el contenido de `/tmp/bed-test/key1.txt` (formato `[fp/path]xpub.../<0;1>/*`).
       - Pegar en el textarea xpub.
       - Verificar que el botón "Descifrar" se HABILITA (esto era lo que estaba roto).

    6. Pulsar "Descifrar".
       - Esperar resultado.
       - Verificar que aparece el descriptor descifrado (NO un error 422 DESCRIPTOR_PARSE).
       - Comparar el output con el contenido de `/tmp/bed-test/desc.txt` (debe coincidir el descriptor 2-of-3 sortedmulti original).

    7. Test de regresión bare:
       - Limpiar la xpub (ya se limpia automáticamente tras descifrar exitoso).
       - Recargar `/tmp/bed-test/wallet.bed`.
       - Pegar la xpub bare ejemplo: `xpub6PLACEHOLDER2xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx` (extraída de key1.txt sin prefix/sufijo, o usar `sed -E 's/^\[[^]]*\]//; s|/.*$||' /tmp/bed-test/key1.txt`).
       - Pulsar Descifrar; debe funcionar igual que antes (regresión cero).

    8. Test de rechazo:
       - Pegar texto basura (ej. "hola"). Botón Descifrar debe quedar disabled.
       - Pegar solo `[fp/path]` sin xpub. Botón Descifrar debe quedar disabled.
  </how-to-verify>
  <resume-signal>Escribir "approved" si los pasos 4-8 funcionan como se describe, o describir cualquier discrepancia (especialmente: ¿el descriptor descifrado coincide con desc.txt? ¿el botón se habilita con descriptor-style? ¿bare sigue funcionando?).</resume-signal>
</task>

</tasks>

<verification>
- [ ] `xpub.js` exporta `normalizeXpub` y `validateXpub` acepta descriptor-style.
- [ ] `TabDescifrar.svelte` llama `normalizeXpub` antes del `formData.append('xpub', ...)`.
- [ ] `npm run build` en frontend completa sin errores.
- [ ] Smoke test manual: descifrar wallet.bed con key1.txt (descriptor-style) devuelve el descriptor original.
- [ ] Regresión: descifrar wallet.bed con xpub bare sigue funcionando.
- [ ] Backend Rust NO modificado (verificar con `git diff src/` vacío fuera de frontend/).
</verification>

<success_criteria>
1. Pegar xpub descriptor-style de Liana habilita el botón Descifrar (antes lo dejaba disabled).
2. Round-trip end-to-end: `wallet.bed` + key1.txt (descriptor-style) → descriptor descifrado igual al de desc.txt.
3. xpub bare clásica sigue funcionando sin regresión.
4. Build de frontend limpio.
5. Backend Rust intacto (Phase 1 stable contract preservado).
</success_criteria>

<output>
After completion, create `.planning/quick/260506-rx9-260506-rgb-fix-xpub-interop-con-liana/260506-rx9-SUMMARY.md` documenting:
- Cambios concretos en xpub.js (regex final, función normalizeXpub).
- Decisión de strip permisivo (`/.*$`) y por qué es seguro (xpub base58 sin `/`).
- Resultado del smoke test manual.
- Confirmación de que backend no se tocó.
</output>
