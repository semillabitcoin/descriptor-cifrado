---
quick_id: 260507-v6e
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/src/validate.rs
  - crates/core/src/encrypt.rs
  - crates/core/tests/validate.rs
  - frontend/src/components/TabCifrar.svelte
  - frontend/src/components/TabDescifrar.svelte
  - CLAUDE.md
  - .planning/PROJECT.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "cargo test -p bed-core validate pasa los 7 tests sin error"
    - "POST /api/encrypt con descriptor <0;1>/* devuelve 200"
    - "POST /api/encrypt con descriptor <2;3>/* devuelve 200 (antes era 422)"
    - "POST /api/encrypt con descriptor <5;5>/* devuelve 422 MISSING_MULTIPATH_WILDCARD"
    - "TabCifrar tiene dropzone + botón 'Subir archivo con descriptor' que carga .txt en el textarea"
    - "TabCifrar tiene botón 'Limpiar' junto a 'Cifrar' que resetea todo el estado local"
    - "TabDescifrar tiene botón 'Limpiar todo' (siempre visible) que resetea todos los campos e inputs"
    - "npm run build en frontend no produce errores"
  artifacts:
    - path: "crates/core/src/validate.rs"
      provides: "función require_multipath_wildcard (renombrada) con lógica relajada"
    - path: "crates/core/tests/validate.rs"
      provides: "7 tests actualizados con nuevos nombres y aserciones"
    - path: "frontend/src/components/TabCifrar.svelte"
      provides: "dropzone, handler de archivo, botón Limpiar, texto de ayuda actualizado"
    - path: "frontend/src/components/TabDescifrar.svelte"
      provides: "botón Limpiar todo siempre visible con reset completo de estado"
  key_links:
    - from: "crates/core/src/encrypt.rs"
      to: "crates/core/src/validate.rs"
      via: "use crate::validate::require_multipath_wildcard"
      pattern: "require_multipath_wildcard"
    - from: "crates/core/tests/validate.rs"
      to: "bed_core::validate"
      via: "use bed_core::validate::require_multipath_wildcard"
      pattern: "require_multipath_wildcard"
---

<objective>
Tres mejoras atómicas al proyecto BED Start9:

1. Relajar el validador multipath de `<0;1>/*` estricto a cualquier par distinto `<a;b>/*`, para interoperabilidad con Liana recovery paths (`<2;3>/*`).
2. Añadir carga de descriptor desde archivo en TabCifrar (dropzone + botón), más botón "Limpiar" por tab.
3. Actualizar textos de ayuda y constraints de docs para reflejar la nueva regla.

Propósito: Liana usa `<2;3>/*` en paths de recuperación; el validador actual rechazaba estos descriptors válidos con 422.
Salida: Validador relajado + UI mejorada en ambos tabs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@crates/core/src/validate.rs
@crates/core/src/encrypt.rs
@crates/core/tests/validate.rs
@frontend/src/components/TabCifrar.svelte
@frontend/src/components/TabDescifrar.svelte

<interfaces>
<!-- Estado actual del validador — se reemplaza en Task 1 -->
<!-- validate.rs: función require_multipath_0_1 con check paths[0]=="0" && paths[1]=="1" -->
<!-- encrypt.rs línea 14: use crate::validate::require_multipath_0_1 -->
<!-- encrypt.rs línea 31: require_multipath_0_1(&desc)?; -->
<!-- tests/validate.rs línea 4: use bed_core::validate::require_multipath_0_1; -->
<!-- tests/validate.rs: 6 tests, todos llaman require_multipath_0_1(&d) -->

<!-- TabDescifrar.svelte — patrón a replicar en TabCifrar -->
<!-- Dropzone: líneas 127-156 (div.dropzone con drag/drop/click + input[type=file] hidden) -->
<!-- Botón subir xpub: líneas 197-206 (btn-ghost + input[type=file] hidden) -->
<!-- Estado: bedFile, bedFilename, dragOver, bedFileInput ref -->
<!-- Handlers: handleDragOver, handleDragLeave, handleDrop, acceptBedFile, clearBedFile -->
<!-- CSS .dropzone, .dropzone:hover, .dropzone.dragover, .dropzone-text, .filename, .separator -->

<!-- TabCifrar.svelte — estado actual -->
<!-- Variables: descriptor, result, loading, errorVisible, errorMessage, warningToast, warningMessage -->
<!-- Botón submit: líneas 83-89 — único botón, disabled={!descriptor.trim() || loading} -->
<!-- No tiene ref a file input, no tiene dragOver, no tiene file handlers -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relajar validador multipath + actualizar tests + callsite</name>
  <files>
    crates/core/src/validate.rs
    crates/core/src/encrypt.rs
    crates/core/tests/validate.rs
  </files>
  <action>
**validate.rs** — reemplazar completamente con la versión relajada:

```rust
//! Validación de wildcard multipath BIP (CORE-03, D-08).
//!
//! La crate bitcoin-encrypted-backup NO enforce esto — acepta cualquier descriptor
//! con al menos una clave no-NUMS. Este módulo es el guard de capa de aplicación
//! que rechaza descriptors que expondrían la xpub on-chain al primer gasto.

use bitcoin_encrypted_backup::miniscript::{
    descriptor::Wildcard, Descriptor, DescriptorPublicKey, ForEachKey,
};

use crate::CoreError;

/// Require que cada clave sea un `MultiXPub` con exactamente 2 paths distintos
/// y `Wildcard::Unhardened`. Acepta cualquier par `<a;b>/*` con `a ≠ b`
/// (por ejemplo `<0;1>/*`, `<2;3>/*`). Rechaza bare xpubs, single wildcards
/// y pares degenerados como `<5;5>/*`.
pub fn require_multipath_wildcard(desc: &Descriptor<DescriptorPublicKey>) -> Result<(), CoreError> {
    let mut all_ok = true;

    desc.for_each_key(|k| {
        let ok = match k {
            DescriptorPublicKey::MultiXPub(mx) => {
                if mx.wildcard != Wildcard::Unhardened {
                    false
                } else {
                    let paths = mx.derivation_paths.paths();
                    paths.len() == 2 && paths[0] != paths[1]
                }
            }
            _ => false, // Single, XPub (single wildcard o ninguno) → rechazar
        };
        if !ok {
            all_ok = false;
        }
        true // continuar iteración
    });

    if all_ok {
        Ok(())
    } else {
        Err(CoreError::MissingMultipathWildcard)
    }
}
```

Cambios clave respecto a la versión anterior:
- Nombre de función: `require_multipath_0_1` → `require_multipath_wildcard`
- Condición eliminada: `paths[0].to_string() == "0" && paths[1].to_string() == "1"`
- Condición nueva: `paths[0] != paths[1]` (rechaza pares degenerados `<5;5>/*`)
- Comentario de módulo y doc-comment actualizados en castellano

**encrypt.rs** — actualizar únicamente el import y el call site (líneas 14 y 31):
- Línea 14: cambiar `use crate::validate::require_multipath_0_1` → `use crate::validate::require_multipath_wildcard`
- Línea 31: cambiar `require_multipath_0_1(&desc)?` → `require_multipath_wildcard(&desc)?`
- También actualizar el doc-comment de `encrypt_descriptor` (línea 25): cambiar "Valida `<0;1>/*` primero" → "Valida multipath wildcard primero"

**crates/core/tests/validate.rs** — actualizar completamente:

```rust
#![allow(clippy::panic)]

use std::str::FromStr;

use bed_core::miniscript::{Descriptor, DescriptorPublicKey};
use bed_core::validate::require_multipath_wildcard;
use bed_core::CoreError;

fn parse(s: &str) -> Descriptor<DescriptorPublicKey> {
    Descriptor::<DescriptorPublicKey>::from_str(s)
        .unwrap_or_else(|e| panic!("fixture parse failed: {s} -> {e}"))
}

const VALID_FIXTURE: &str = include_str!("fixtures/desc.txt");

// xpubs reales de los fixtures (datos de test no secretos)
const XPUB_A: &str = "xpub6PLACEHOLDER2xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx";
const XPUB_B: &str = "xpub6DhWabCb9j5vGn3VU2YJ9kNgk258AoBcQWHQPstRY2bYZJjJUDvxTZPrg58An3CCEWhhVuxk9A6nobikMQjtK8Xk5JakJc1HSu13Z3dKfpo";

#[test]
fn rejects_bare_xpub() {
    let d = parse(&format!("wsh(pk({XPUB_A}))"));
    assert!(matches!(
        require_multipath_wildcard(&d),
        Err(CoreError::MissingMultipathWildcard)
    ));
}

#[test]
fn rejects_single_wildcard() {
    let d = parse(&format!("wsh(pk({XPUB_A}/0/*))"));
    assert!(matches!(
        require_multipath_wildcard(&d),
        Err(CoreError::MissingMultipathWildcard)
    ));
}

#[test]
fn rejects_duplicate_multipath_indices() {
    // <5;5>/* es degenerado — ambos paths iguales, se rechaza
    let d = parse(&format!("wsh(pk({XPUB_A}/<5;5>/*))"));
    assert!(matches!(
        require_multipath_wildcard(&d),
        Err(CoreError::MissingMultipathWildcard)
    ));
}

#[test]
fn accepts_valid_fixture() {
    let d = parse(VALID_FIXTURE.trim());
    assert!(
        require_multipath_wildcard(&d).is_ok(),
        "el fixture válido debe pasar"
    );
}

#[test]
fn accepts_synthetic_2_of_3_multipath() {
    // Triple-key sortedmulti, todos <0;1>/*
    let d = parse(&format!(
        "wsh(sortedmulti(2,{XPUB_A}/<0;1>/*,{XPUB_B}/<0;1>/*,{XPUB_A}/<0;1>/*))"
    ));
    assert!(require_multipath_wildcard(&d).is_ok());
}

#[test]
fn accepts_alternate_multipath_indices() {
    // <2;3>/* es válido — Liana usa este par para recovery paths
    let d = parse(&format!("wsh(pk({XPUB_A}/<2;3>/*))"));
    assert!(require_multipath_wildcard(&d).is_ok());
}

#[test]
fn accepts_mixed_multipath_indices() {
    // Mix <0;1> + <2;3> — válido, cada clave tiene par distinto
    let d = parse(&format!(
        "wsh(sortedmulti(2,{XPUB_A}/<0;1>/*,{XPUB_B}/<2;3>/*))"
    ));
    assert!(require_multipath_wildcard(&d).is_ok());
}
```

Cambios vs versión anterior:
- Import: `require_multipath_0_1` → `require_multipath_wildcard`
- Todos los call sites: `require_multipath_0_1(&d)` → `require_multipath_wildcard(&d)`
- `rejects_wrong_multipath_indices` → renombrado a `accepts_alternate_multipath_indices`, aserción invertida (ahora `.is_ok()`)
- `rejects_mixed_one_good_one_bad` → renombrado a `accepts_mixed_multipath_indices`, aserción invertida (ahora `.is_ok()`)
- Nuevo test `rejects_duplicate_multipath_indices` con `<5;5>/*` → `Err(MissingMultipathWildcard)`
- `#![allow(clippy::panic)]` movido a nivel de archivo (no solo `#[allow]` por función)

NOTA: XPUB_A en el archivo actual es un placeholder intencionado ("xpub6PLACEHOLDER2xxx...") — mantenerlo exactamente igual. El test `rejects_bare_xpub` y `rejects_single_wildcard` parsean con él; si miniscript rechaza el placeholder, esos tests ya fallaban antes. Verificar con `cargo test` que todos pasan.
  </action>
  <verify>
    <automated>cd /home/anon/descriptor-cifrado && cargo test -p bed-core validate 2>&1</automated>
  </verify>
  <done>
    - 7 tests en crates/core/tests/validate.rs pasan sin error
    - `cargo test -p bed-core validate` termina con "test result: ok. 7 passed"
    - No hay referencias a `require_multipath_0_1` en el codebase (grep confirma cero hits)
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend — dropzone + Limpiar en TabCifrar, Limpiar todo en TabDescifrar, copy updates</name>
  <files>
    frontend/src/components/TabCifrar.svelte
    frontend/src/components/TabDescifrar.svelte
    CLAUDE.md
    .planning/PROJECT.md
  </files>
  <action>
**TabCifrar.svelte** — reescribir completo con las siguientes adiciones:

SECCIÓN `<script>` — añadir al estado existente:
```javascript
let descriptorDragOver = $state(false);
let descriptorFilename = $state('');
let descriptorFileInput;
```

Añadir handlers de archivo (después de los handlers existentes, antes de `handleSubmit`):
```javascript
function handleDescriptorDragOver(e) {
  e.preventDefault();
  descriptorDragOver = true;
}
function handleDescriptorDragLeave() {
  descriptorDragOver = false;
}
async function handleDescriptorDrop(e) {
  e.preventDefault();
  descriptorDragOver = false;
  const file = e.dataTransfer?.files?.[0];
  if (file) await loadDescriptorFile(file);
}
async function handleDescriptorFilePick(e) {
  const file = e.target?.files?.[0];
  if (!file) return;
  await loadDescriptorFile(file);
  if (descriptorFileInput) descriptorFileInput.value = '';
}
async function loadDescriptorFile(file) {
  try {
    const text = await file.text();
    descriptor = text.trim();
    descriptorFilename = file.name;
  } catch {
    errorMessage = 'No se pudo leer el archivo de descriptor.';
    errorVisible = true;
  }
}
```

Añadir función `handleLimpiar`:
```javascript
function handleLimpiar() {
  descriptor = '';
  result = null;
  loading = false;
  errorVisible = false;
  errorMessage = '';
  warningToast = false;
  warningMessage = '';
  descriptorFilename = '';
  descriptorDragOver = false;
  if (descriptorFileInput) descriptorFileInput.value = '';
}
```

Actualizar `PLACEHOLDER` para mostrar ejemplo realista con paths distintos:
```javascript
const PLACEHOLDER =
  'wsh(or_d(pk([fp/48h/0h/0h/2h]xpub.../<0;1>/*),and_v(v:pkh([fp/48h/0h/0h/2h]xpub.../<2;3>/*),older(N))))#checksum';
```

SECCIÓN `<form>` — la estructura queda así (mantener `<InlineError>` arriba):

```html
<form class="form" onsubmit={handleSubmit} novalidate>
  <InlineError bind:visible={errorVisible} message={errorMessage} />

  <div class="field">
    <label for="descriptor-input" class="label">Descriptor multisig</label>

    <!-- Dropzone para cargar desde archivo -->
    <div
      class="dropzone"
      class:dragover={descriptorDragOver}
      role="button"
      tabindex="0"
      aria-label="Soltar archivo con descriptor aquí o pulsa para seleccionar"
      ondragover={handleDescriptorDragOver}
      ondragleave={handleDescriptorDragLeave}
      ondrop={handleDescriptorDrop}
      onclick={() => descriptorFileInput?.click()}
      onkeydown={(e) => { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); descriptorFileInput?.click(); } }}
    >
      {#if descriptorFilename}
        <p class="dropzone-text">
          Archivo cargado: <strong class="filename">{descriptorFilename}</strong>
        </p>
        <button type="button" class="btn btn-ghost" onclick={(e) => { e.stopPropagation(); descriptorFilename = ''; descriptor = ''; if (descriptorFileInput) descriptorFileInput.value = ''; }}>
          Cambiar archivo
        </button>
      {:else}
        <p class="dropzone-text">Arrastra el archivo con descriptor aquí o pulsa para seleccionar</p>
      {/if}
      <input
        bind:this={descriptorFileInput}
        type="file"
        accept=".txt,.descriptor,text/plain"
        hidden
        onchange={handleDescriptorFilePick}
      />
    </div>

    <p class="separator">— o pega el descriptor —</p>

    <textarea
      id="descriptor-input"
      class="textarea"
      bind:value={descriptor}
      placeholder={PLACEHOLDER}
      spellcheck="false"
      autocomplete="off"
      autocorrect="off"
      autocapitalize="off"
      aria-describedby="descriptor-help"
      rows="6"
    ></textarea>
    <p id="descriptor-help" class="help">
      Pega el descriptor con derivación multipath <code>&lt;a;b&gt;/*</code> (típicamente <code>&lt;0;1&gt;/*</code>; Liana recovery puede usar <code>&lt;2;3&gt;/*</code>). Nada se envía a internet.
    </p>
  </div>

  <div class="btn-row">
    <button type="submit" class="btn btn-primary" disabled={!descriptor.trim() || loading}>
      {#if loading}
        <Spinner /> <span>Cifrando…</span>
      {:else}
        <span>Cifrar</span>
      {/if}
    </button>
    <button
      type="button"
      class="btn btn-ghost"
      disabled={!descriptor && result === null && !errorVisible && !warningToast}
      onclick={handleLimpiar}
    >
      Limpiar
    </button>
  </div>
</form>
```

SECCIÓN `<style>` — añadir al bloque existente (después de `.btn-primary:hover:not(:disabled)`):
```css
  .btn-row {
    display: flex;
    align-items: center;
    gap: var(--space-md);
  }
  .dropzone {
    border: 2px dashed var(--color-border);
    border-radius: var(--radius-card);
    background: var(--color-surface-sunken);
    padding: var(--space-xl) var(--space-md);
    text-align: center;
    cursor: pointer;
    transition: border-color var(--transition-color), background-color var(--transition-color);
  }
  .dropzone:hover, .dropzone:focus-visible {
    border-color: var(--color-border-focus);
    outline: 0;
  }
  .dropzone.dragover {
    border-color: var(--color-accent);
    background: var(--color-surface-raised);
  }
  .dropzone-text {
    margin: 0 0 var(--space-sm) 0;
    font-size: var(--font-size-label);
    color: var(--color-text-secondary);
  }
  .filename {
    font-family: var(--font-mono);
    font-size: var(--font-size-mono);
    color: var(--color-text-primary);
  }
  .separator {
    text-align: center;
    margin: var(--space-sm) 0;
    font-size: var(--font-size-label);
    color: var(--color-text-secondary);
  }
  .btn-ghost {
    background: transparent;
    color: var(--color-text-secondary);
    border: 0;
    align-self: flex-start;
  }
  .btn-ghost:hover { background: var(--color-surface-sunken); color: var(--color-text-primary); }
```

---

**TabDescifrar.svelte** — cambios mínimos (no reescribir el archivo completo):

1. Añadir función `handleLimpiarTodo` en `<script>` (después de `handleClearResult`):
```javascript
function handleLimpiarTodo() {
  bedFile = null;
  bedFilename = '';
  armoredText = '';
  xpubText = '';
  descriptor = null;
  errorVisible = false;
  errorMessage = '';
  dragOver = false;
  if (bedFileInput) bedFileInput.value = '';
  if (xpubFileInput) xpubFileInput.value = '';
}
```

2. Sustituir el bloque del botón submit (líneas ~209-215) por un `div.btn-row` con ambos botones:
```html
  <div class="btn-row">
    <button type="submit" class="btn btn-primary" disabled={!canSubmit}>
      {#if loading}
        <Spinner /> <span>Descifrando…</span>
      {:else}
        <span>Descifrar</span>
      {/if}
    </button>
    <button type="button" class="btn btn-ghost" onclick={handleLimpiarTodo}>
      Limpiar todo
    </button>
  </div>
```

3. Eliminar el bloque `{#if descriptor}...<button>Limpiar resultado</button>...{/if}` (líneas 218-225) y sustituir por solo mostrar el resultado sin el botón de limpiar (el botón "Limpiar todo" ya cubre esa función):
```html
{#if descriptor}
  <DescifrarOutputs {descriptor} />
{/if}
```

4. Añadir en `<style>` (al final, antes del cierre `</style>`):
```css
  .btn-row {
    display: flex;
    align-items: center;
    gap: var(--space-md);
  }
```

5. Eliminar la regla `.clear-btn` y `.result-wrapper` que ya no se usan (o dejarlas — no causan error).

---

**CLAUDE.md** — buscar la línea que dice:
```
- **BIP**: descriptors deben usar derivación `<0;1>/*`; sin esto, gastar desde dirección 0 expone la xpub on-chain y rompe el cifrado
```
Y reemplazarla por:
```
- **BIP**: descriptors deben usar derivación multipath `<a;b>/*` con `a≠b` (típicamente `<0;1>/*`; Liana recovery usa `<2;3>/*`); sin esta derivación, gastar desde dirección 0 expone la xpub on-chain y rompe el cifrado
```

**NOTA**: CLAUDE.md está en `/home/anon/CLAUDE.md` (raíz del home, no del proyecto). Actualizar esa línea en la sección de Constraints correspondiente.

---

**.planning/PROJECT.md** — buscar la línea de Constraints:
```
- **BIP**: descriptors deben usar derivación `<0;1>/*`; sin esto, gastar desde dirección 0 expone la xpub on-chain y rompe el cifrado
```
Y reemplazarla por:
```
- **BIP**: descriptors deben usar derivación multipath `<a;b>/*` con `a≠b` (típicamente `<0;1>/*`; Liana recovery usa `<2;3>/*`); sin esta derivación, gastar desde dirección 0 expone la xpub on-chain y rompe el cifrado
```

También en la sección Requirements > Validated, la línea:
```
- [x] Validación BIP: rechazar descriptors sin derivación `<0;1>/*` *(Validado en Phase 1: crypto-core-http-api — substrato HTTP devuelve 422 MISSING_MULTIPATH_WILDCARD)*
```
Actualizar a:
```
- [x] Validación BIP: rechazar descriptors sin derivación multipath `<a;b>/*` con `a≠b` *(Validado en Phase 1: crypto-core-http-api — substrato HTTP devuelve 422 MISSING_MULTIPATH_WILDCARD; relajado en quick task 260507-v6e para aceptar cualquier par distinto, no solo `<0;1>/*`)*
```
  </action>
  <verify>
    <automated>cd /home/anon/descriptor-cifrado/frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `npm run build` termina sin errores
    - TabCifrar renderiza dropzone arriba del textarea con texto "Arrastra el archivo con descriptor aquí o pulsa para seleccionar"
    - Botón "Limpiar" visible junto a "Cifrar" en TabCifrar
    - TabDescifrar tiene botón "Limpiar todo" junto a "Descifrar" (siempre visible, no condicionado a `{#if descriptor}`)
    - Texto de ayuda en TabCifrar menciona `<a;b>/*` y `<2;3>/*`
    - CLAUDE.md y PROJECT.md actualizados con la nueva regla multipath genérica
  </done>
</task>

</tasks>

<verification>
Tras ejecutar ambas tasks, verificar el conjunto completo:

```bash
# 1. Tests Rust
cd /home/anon/descriptor-cifrado && cargo test -p bed-core validate 2>&1

# 2. Build Rust limpio
cargo build --release 2>&1 | tail -5

# 3. Build frontend
cd /home/anon/descriptor-cifrado/frontend && npm run build 2>&1 | tail -10

# 4. Smoke test — iniciar servidor
BED_DATA_DIR=/tmp/bed-uat-data RUST_LOG=info /home/anon/descriptor-cifrado/target/release/bed-server &
sleep 2

# 5. Smoke test descriptor <0;1>/* (debe seguir siendo 200)
curl -s -o /dev/null -w "%{http_code}" -X POST http://127.0.0.1:8080/api/encrypt \
  -H 'Content-Type: application/json' \
  -d '{"descriptor":"wsh(sortedmulti(2,[68a9ec24/48h/0h/0h/2h]xpub6PLACEHOLDER2xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx/<0;1>/*,[f91be7a4/48h/0h/0h/2h]xpub6DhWabCb9j5vGn3VU2YJ9kNgk258AoBcQWHQPstRY2bYZJjJUDvxTZPrg58An3CCEWhhVuxk9A6nobikMQjtK8Xk5JakJc1HSu13Z3dKfpo/<0;1>/*))"}'
# Esperado: 200

# 6. Verificar que no quedan referencias a la función antigua
grep -r "require_multipath_0_1" /home/anon/descriptor-cifrado/crates/
# Esperado: sin resultados

# 7. Apagar servidor de prueba
kill %1 2>/dev/null || true
```
</verification>

<success_criteria>
- `cargo test -p bed-core validate` → "test result: ok. 7 passed; 0 failed"
- `cargo build --release` → sin errores
- `npm run build` (frontend/) → sin errores
- Cero referencias a `require_multipath_0_1` en el codebase
- TabCifrar tiene dropzone funcional + botón "Limpiar"
- TabDescifrar tiene botón "Limpiar todo" siempre visible
- CLAUDE.md y .planning/PROJECT.md mencionan `<a;b>/*` en lugar de `<0;1>/*` solo
</success_criteria>

<output>
Tras completar ambas tasks, crear el summary en:
.planning/quick/260507-v6e-relax-multipath-validator-file-upload-ta/260507-v6e-SUMMARY.md

Con formato:
- Qué se cambió y por qué
- Commits creados (hash + mensaje)
- Tests antes/después
- Comportamiento nuevo vs antiguo del validador
</output>
