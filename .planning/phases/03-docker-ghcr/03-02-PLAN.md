---
phase: 03-docker-ghcr
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - .github/workflows/docker.yml
autonomous: false
requirements: [PKG-02, PKG-03, PKG-04]
must_haves:
  truths:
    - "Push a `main` dispara el workflow `Docker` en GitHub Actions y publica `ghcr.io/semillabitcoin/descriptor-cifrado:latest` y `:sha-<7chars>`"
    - "El manifest de la imagen publicada contiene entries para `linux/amd64` Y `linux/arm64`"
    - "El job `ldd-check` falla con exit 1 si el binario release linka contra `libssl`/`libcrypto`/`native-tls`/`libpq`/`libmysqlclient`/`libsqlite3`"
    - "Tras el primer push, `docker pull ghcr.io/semillabitcoin/descriptor-cifrado:latest` desde una máquina sin `docker login` funciona (el package es público)"
    - "PRs corren build-only sin push (validan que el Dockerfile sigue compilando) y NO publican nada en GHCR"
  artifacts:
    - path: ".github/workflows/docker.yml"
      provides: "Workflow Docker con 3 jobs: build-and-push, ldd-check, make-public"
      contains: "ghcr.io/semillabitcoin/descriptor-cifrado"
  key_links:
    - from: ".github/workflows/docker.yml job build-and-push"
      to: "Dockerfile en raíz"
      via: "docker/build-push-action context: ."
      pattern: "context: \\."
    - from: ".github/workflows/docker.yml job build-and-push"
      to: "GHCR registry ghcr.io/semillabitcoin/descriptor-cifrado"
      via: "docker/login-action + docker/metadata-action images: line"
      pattern: "ghcr\\.io/semillabitcoin/descriptor-cifrado"
    - from: ".github/workflows/docker.yml job ldd-check"
      to: "target/release/bed-server"
      via: "cargo build --release --locked + ldd + grep -E 'libssl|libcrypto|...'"
      pattern: "ldd target/release/bed-server"
    - from: ".github/workflows/docker.yml job make-public"
      to: "GHCR API /orgs/semillabitcoin/packages/container/descriptor-cifrado"
      via: "gh api -X PATCH visibility=public"
      pattern: "/orgs/semillabitcoin/packages/container/descriptor-cifrado"
---

<objective>
Crear `.github/workflows/docker.yml` con 3 jobs (build-and-push multi-arch, ldd-check, make-public) que publica `ghcr.io/semillabitcoin/descriptor-cifrado` multi-arch tras push a main, valida que el binario nativo amd64 no linka contra libssl, y marca el package como público.

Purpose: Cumplir PKG-02 (multi-arch GHCR publish), PKG-03 (ldd check), PKG-04 (GHCR público). Sin esto, la imagen del Plan 03-01 nunca llega a un registry y Phase 4 (s9pk) no puede pull-earla.
Output: Un único workflow `.github/workflows/docker.yml` independiente de `ci.yml`. Tras merge a main, publica imagen multi-arch en GHCR pública.

**Nota sobre PKG-04:** Hay incertidumbre documentada en RESEARCH.md sobre el endpoint API PATCH para paquetes de organización. La estrategia es:
1. Mecanismo primario: herencia automática de visibilidad desde repo público + label `org.opencontainers.image.source` (configurada en metadata-action).
2. Best-effort secondary: step `make-public` con `continue-on-error: true` que llama `PATCH /orgs/semillabitcoin/packages/container/descriptor-cifrado`.
3. Fallback manual: documentar en SUMMARY que si el package queda privado tras primer push, el owner debe togglear manualmente en `github.com/orgs/semillabitcoin/packages/container/descriptor-cifrado/settings`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-docker-ghcr/03-CONTEXT.md
@.planning/phases/03-docker-ghcr/03-RESEARCH.md
@.planning/phases/03-docker-ghcr/03-01-SUMMARY.md
@.github/workflows/ci.yml
@Dockerfile

<interfaces>
<!-- Workflow versions actuales (Mayo 2026, verificadas en RESEARCH.md sección "Standard Stack") -->

Versiones actuales de Docker GHA actions (NO usar las v3/v5/v6 mencionadas en CONTEXT.md — esas son anteriores a Marzo 2026):

| Action | Versión | Notas |
|--------|---------|-------|
| `actions/checkout` | `@v4` | Mismo que ci.yml |
| `docker/setup-qemu-action` | `@v4` | Marzo 2026, Node 24 runtime |
| `docker/setup-buildx-action` | `@v4` | Marzo 2026, Node 24 |
| `docker/login-action` | `@v4` | Marzo-Abril 2026 |
| `docker/metadata-action` | `@v6` | Marzo 2026, ESM migration |
| `docker/build-push-action` | `@v7` | Abril 2026, requires Actions Runner v2.327.1+ |
| `dtolnay/rust-toolchain` | `@stable` | Mismo que ci.yml |
| `Swatinem/rust-cache` | `@v2` | Mismo que ci.yml |

Patrón GHA cache para buildx (de RESEARCH.md "Pattern 2"):
```yaml
cache-from: type=gha
cache-to: type=gha,mode=max
```
`mode=max` cachea TODOS los stages intermedios; sin él, solo el stage final se cachea.

GHCR auth pattern (de RESEARCH.md):
```yaml
- name: Log in to GHCR
  if: github.event_name != 'pull_request'
  uses: docker/login-action@v4
  with:
    registry: ghcr.io
    username: ${{ github.actor }}
    password: ${{ secrets.GITHUB_TOKEN }}
```
El `if:` es crítico — PRs no tienen scope `packages: write`, fallarían sin él.

Permissions del workflow (D-11):
```yaml
permissions:
  contents: read
  packages: write
```
Sin `id-token` (no hay OIDC en v1). Sin `pull-requests` (no comentamos en PRs).

Patrón ci.yml existente (verificado, NO se modifica en este plan):
- 5 jobs: fmt, clippy, test, audit, deny
- Triggers: pull_request + push branches: [main]
- Usa `dtolnay/rust-toolchain@stable` + `Swatinem/rust-cache@v2`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Crear .github/workflows/docker.yml con 3 jobs</name>
  <files>.github/workflows/docker.yml</files>
  <read_first>
    - .github/workflows/ci.yml (pattern Actions versions, naming jobs, indentación)
    - .planning/phases/03-docker-ghcr/03-CONTEXT.md (D-09, D-10, D-11)
    - .planning/phases/03-docker-ghcr/03-RESEARCH.md (Pattern 2: docker.yml Workflow + sección "GHCR Visibility — Investigación Crítica" + Pitfalls 1, 3, 6)
    - .planning/phases/03-docker-ghcr/03-01-SUMMARY.md (referencia tamaño imagen amd64 nativa local)
    - Dockerfile (referencia de path y nombre de binario)
  </read_first>
  <action>
    Crear nuevo archivo `.github/workflows/docker.yml` con el siguiente contenido EXACTO. Implementa D-09, D-10, D-11 con las versiones actuales de actions (Mayo 2026) verificadas en RESEARCH.md:

    ```yaml
    name: Docker

    on:
      push:
        branches: [main]
        tags: ['v*.*.*']
      pull_request:
        branches: [main]

    permissions:
      contents: read
      packages: write

    jobs:
      build-and-push:
        runs-on: ubuntu-latest
        timeout-minutes: 40
        steps:
          - uses: actions/checkout@v4

          - name: Set up QEMU
            uses: docker/setup-qemu-action@v4

          - name: Set up Docker Buildx
            uses: docker/setup-buildx-action@v4

          - name: Log in to GHCR
            if: github.event_name != 'pull_request'
            uses: docker/login-action@v4
            with:
              registry: ghcr.io
              username: ${{ github.actor }}
              password: ${{ secrets.GITHUB_TOKEN }}

          - name: Extract metadata
            id: meta
            uses: docker/metadata-action@v6
            with:
              images: ghcr.io/semillabitcoin/descriptor-cifrado
              flavor: |
                latest=false
              tags: |
                type=ref,event=branch,enable=${{ github.ref == 'refs/heads/main' }},value=latest
                type=sha,format=short,prefix=sha-
                type=semver,pattern={{version}}
                type=semver,pattern={{major}}.{{minor}}
                type=semver,pattern={{major}}
              labels: |
                org.opencontainers.image.source=https://github.com/semillabitcoin/descriptor-cifrado
                org.opencontainers.image.description=BED — Bitcoin Encrypted Backup app for StartOS
                org.opencontainers.image.licenses=MIT

          - name: Build and push
            uses: docker/build-push-action@v7
            with:
              context: .
              platforms: linux/amd64,linux/arm64
              push: ${{ github.event_name != 'pull_request' }}
              tags: ${{ steps.meta.outputs.tags }}
              labels: ${{ steps.meta.outputs.labels }}
              cache-from: type=gha
              cache-to: type=gha,mode=max

      ldd-check:
        runs-on: ubuntu-latest
        timeout-minutes: 20
        steps:
          - uses: actions/checkout@v4
          - uses: dtolnay/rust-toolchain@stable
          - uses: Swatinem/rust-cache@v2
          - name: Build release binary (native amd64)
            run: cargo build --release --locked --bin bed-server
          - name: ldd check — no libssl or non-distroless libs
            run: |
              echo "=== ldd output ==="
              ldd target/release/bed-server
              echo "=== checking for forbidden libs ==="
              if ldd target/release/bed-server | grep -E 'libssl|libcrypto|native-tls|libpq|libmysqlclient|libsqlite3'; then
                echo "ERROR: Binary links to forbidden library"
                exit 1
              fi
              echo "=== ldd check PASSED ==="

      make-public:
        runs-on: ubuntu-latest
        needs: build-and-push
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        continue-on-error: true
        steps:
          - name: Make GHCR package public (org endpoint, best-effort)
            env:
              GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
            run: |
              echo "Attempting PATCH /orgs/semillabitcoin/packages/container/descriptor-cifrado"
              if gh api -X PATCH \
                /orgs/semillabitcoin/packages/container/descriptor-cifrado \
                -f visibility=public; then
                echo "Package set to public via API."
              else
                echo "WARNING: API call failed. Package visibility falls back to repo inheritance."
                echo "If package is still private after first push, toggle manually at:"
                echo "  https://github.com/orgs/semillabitcoin/packages/container/descriptor-cifrado/settings"
                echo "(Visibility inheritance from public repo + org.opencontainers.image.source label is the primary mechanism — see Plan 03-02 Task 1.)"
              fi
    ```

    Decisiones críticas implementadas:
    - **Versiones actuales (Mayo 2026):** `setup-qemu@v4`, `setup-buildx@v4`, `login@v4`, `metadata@v6`, `build-push@v7`. RESEARCH.md confirma estas son las versiones latest (CONTEXT.md tenía v3/v5/v6 anticuadas).
    - **`flavor: latest=false`** + tag explícito `type=ref,event=branch,enable=${{ github.ref == 'refs/heads/main' }},value=latest`: produce `latest` SOLO en push a main, no en otras branches (Pitfall implícito: metadata-action v6 default genera latest en cualquier branch).
    - **Triple mecanismo PKG-04:** label `org.opencontainers.image.source` en metadata (herencia primaria) + step `make-public` con `continue-on-error: true` (best-effort) + fallback manual documentado en logs.
    - **`ldd-check`** usa `grep -E ... ; then exit 1` porque `ldd` por sí solo no falla nunca (Pitfall 6).
    - **`timeout-minutes: 40`** en build-and-push: arm64 emulación QEMU cold start estimado 10-20 min (Open Question 3 de RESEARCH.md).
    - **Permissions explícitas:** `contents: read` + `packages: write`, sin id-token (D-11).
    - **NO modificar `ci.yml`:** ese workflow queda Rust-only (D-09).

    Tras crear el archivo, validar sintaxis YAML localmente:
    ```bash
    python3 -c "import yaml; yaml.safe_load(open('.github/workflows/docker.yml'))" && echo "YAML OK"
    ```
  </action>
  <verify>
    <automated>test -f .github/workflows/docker.yml &amp;&amp; python3 -c "import yaml; yaml.safe_load(open('.github/workflows/docker.yml'))" &amp;&amp; grep -F 'name: Docker' .github/workflows/docker.yml &amp;&amp; grep -F 'docker/setup-qemu-action@v4' .github/workflows/docker.yml &amp;&amp; grep -F 'docker/setup-buildx-action@v4' .github/workflows/docker.yml &amp;&amp; grep -F 'docker/login-action@v4' .github/workflows/docker.yml &amp;&amp; grep -F 'docker/metadata-action@v6' .github/workflows/docker.yml &amp;&amp; grep -F 'docker/build-push-action@v7' .github/workflows/docker.yml &amp;&amp; grep -F 'platforms: linux/amd64,linux/arm64' .github/workflows/docker.yml &amp;&amp; grep -F 'ghcr.io/semillabitcoin/descriptor-cifrado' .github/workflows/docker.yml &amp;&amp; grep -F 'cache-to: type=gha,mode=max' .github/workflows/docker.yml &amp;&amp; grep -F 'libssl|libcrypto|native-tls|libpq|libmysqlclient|libsqlite3' .github/workflows/docker.yml &amp;&amp; grep -F '/orgs/semillabitcoin/packages/container/descriptor-cifrado' .github/workflows/docker.yml &amp;&amp; grep -F 'continue-on-error: true' .github/workflows/docker.yml &amp;&amp; grep -F 'org.opencontainers.image.source=https://github.com/semillabitcoin/descriptor-cifrado' .github/workflows/docker.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/docker.yml` existe
    - YAML válido (`python3 -c "import yaml; yaml.safe_load(...)"` exit 0)
    - Workflow se llama `Docker` (`grep 'name: Docker'`)
    - Triggers: push branches `[main]` + tags `v*.*.*` + pull_request branches `[main]`
    - `permissions:` block contiene `contents: read` y `packages: write`
    - 3 jobs: `build-and-push`, `ldd-check`, `make-public`
    - **Versions exactas (Mayo 2026):**
      - `docker/setup-qemu-action@v4`
      - `docker/setup-buildx-action@v4`
      - `docker/login-action@v4`
      - `docker/metadata-action@v6`
      - `docker/build-push-action@v7`
      - `actions/checkout@v4`
      - `dtolnay/rust-toolchain@stable`
      - `Swatinem/rust-cache@v2`
    - `platforms: linux/amd64,linux/arm64` presente en build-push step
    - `images: ghcr.io/semillabitcoin/descriptor-cifrado` en metadata-action
    - `cache-from: type=gha` y `cache-to: type=gha,mode=max` en build-push
    - `push: ${{ github.event_name != 'pull_request' }}` en build-push (PRs no pushean)
    - login-action tiene `if: github.event_name != 'pull_request'`
    - metadata-action labels incluye `org.opencontainers.image.source=https://github.com/semillabitcoin/descriptor-cifrado` (mecanismo primario PKG-04)
    - `ldd-check` job contiene `cargo build --release --locked --bin bed-server`
    - `ldd-check` script contiene `grep -E 'libssl|libcrypto|native-tls|libpq|libmysqlclient|libsqlite3'` seguido de `exit 1`
    - `make-public` job tiene `if: github.event_name == 'push' && github.ref == 'refs/heads/main'`
    - `make-public` job tiene `needs: build-and-push`
    - `make-public` job tiene `continue-on-error: true`
    - `make-public` step usa endpoint `/orgs/semillabitcoin/packages/container/descriptor-cifrado` (NO `/user/packages/...`)
    - `flavor: latest=false` presente en metadata-action (evita latest en branches no-main)
    - `tags:` incluye `type=ref,event=branch,enable=${{ github.ref == 'refs/heads/main' }},value=latest`
    - `tags:` incluye `type=sha,format=short,prefix=sha-`
    - `tags:` incluye 3 entries `type=semver,pattern={{...}}`
    - `timeout-minutes: 40` en build-and-push
    - `ci.yml` NO modificado (`git diff .github/workflows/ci.yml` vacío)
  </acceptance_criteria>
  <done>
    `.github/workflows/docker.yml` válido y commiteable. Estructura: 3 jobs con versiones actuales (Mayo 2026) de docker/* actions, multi-arch linux/amd64+linux/arm64, ldd-check con grep+exit 1, make-public con endpoint de org y continue-on-error.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificación humana — pre-flight repo público + primer push real a GHCR</name>
  <files>(checkpoint — sin modificación de archivos; valida el workflow recién creado en Task 1 contra GHCR real)</files>
  <read_first>
    - .github/workflows/docker.yml (creado en Task 1)
    - .planning/phases/03-docker-ghcr/03-RESEARCH.md (sección "GHCR Visibility — Investigación Crítica" — mecanismos PKG-04)
    - .planning/phases/03-docker-ghcr/03-CONTEXT.md (sección "Specifics" — test manual end-to-end pre-merge)
    - feedback_ghcr_private_default.md y feedback_test_before_push.md (memoria del usuario)
  </read_first>
  <what-built>
    - `.github/workflows/docker.yml` con 3 jobs: build-and-push (multi-arch), ldd-check, make-public
    - Versions actuales (Mayo 2026): qemu@v4, buildx@v4, login@v4, metadata@v6, build-push@v7
    - Triple mecanismo PKG-04: label image.source + step make-public + fallback manual

    Pendiente: este checkpoint valida que el workflow funciona contra GHCR real (no solo sintaxis YAML).
  </what-built>
  <how-to-verify>
    Este checkpoint requiere un push real a un repo en `semillabitcoin` org de GitHub. Sigue estos pasos en orden:

    **PRE-FLIGHT (CRÍTICO para PKG-04 herencia automática):**

    1. Verificar que el repo `semillabitcoin/descriptor-cifrado` existe Y es PÚBLICO en GitHub:
       ```bash
       gh repo view semillabitcoin/descriptor-cifrado --json visibility,name | jq
       ```
       - Si `"visibility": "PUBLIC"` → continuar
       - Si `"visibility": "PRIVATE"` → ABORTAR este checkpoint. Hacer el repo público primero (Settings → Danger Zone → Change visibility) o aceptar que PKG-04 requerirá toggle manual en UI tras primer push.
       - Si el repo no existe → crear `gh repo create semillabitcoin/descriptor-cifrado --public --source=. --push`. Anotar el comando exacto usado.

    2. Confirmar email noreply en commits locales (`feedback_git_noreply_email.md`):
       ```bash
       git log -1 --format='%ae'
       ```
       Debe ser `55397917+4rkad@users.noreply.github.com`. Si no, configurar antes de push.

    **PUSH Y OBSERVACIÓN:**

    3. Crear branch + commit con todos los cambios de Phase 3 (Plan 01 + Plan 02):
       ```bash
       git status   # Debe listar Cargo.toml, Dockerfile, .dockerignore, .github/workflows/docker.yml
       git add Cargo.toml Dockerfile .dockerignore .github/workflows/docker.yml .planning/phases/03-docker-ghcr/
       git commit -m "feat(phase-3): docker multi-stage + GHCR multi-arch workflow"
       ```

    4. Push a `main` (asume rama actual = main; si trabajas en branch, mergea primero):
       ```bash
       git push origin main
       ```

    5. Observar el workflow en GitHub Actions:
       ```bash
       gh run watch
       # O abrir: https://github.com/semillabitcoin/descriptor-cifrado/actions
       ```

    **VALIDACIONES (Success Criteria del ROADMAP §Phase 3):**

    6. Job `ldd-check` PASA (verde): el binario nativo amd64 NO linka contra libssl/libcrypto/native-tls/etc. **PKG-03 cumplido.**

    7. Job `build-and-push` PASA (verde): la imagen multi-arch se construye y se pushea a GHCR.

    8. Job `make-public` se ejecuta. Resultado esperado: PASS o WARNING (continue-on-error=true). Anotar cuál.

    9. Verificar manifest multi-arch (PKG-02 + Success Criterion 2):
       ```bash
       docker buildx imagetools inspect ghcr.io/semillabitcoin/descriptor-cifrado:latest
       ```
       Debe listar EXPLÍCITAMENTE entries para `linux/amd64` Y `linux/arm64`.

    10. Verificar pull desde máquina sin auth (PKG-04 + Success Criterion 1):
        ```bash
        docker logout ghcr.io 2>/dev/null || true
        docker pull ghcr.io/semillabitcoin/descriptor-cifrado:latest
        ```
        - Si pull funciona → PKG-04 cumplido (sea por herencia automática o por make-public).
        - Si pull devuelve "denied: requested resource is not accessible" → el package quedó privado. Toggle manual:
          ```bash
          # Abrir en navegador:
          open https://github.com/orgs/semillabitcoin/packages/container/descriptor-cifrado/settings
          # Scroll a "Danger Zone" → "Change visibility" → Public → confirmar
          # Reintentar el pull
          ```
          Anotar que se requirió toggle manual (información para SUMMARY).

    11. Verificar tamaño comprimido en GHCR (PKG-01 + Success Criterion 3):
        ```bash
        docker buildx imagetools inspect --raw ghcr.io/semillabitcoin/descriptor-cifrado:latest | \
          python3 -c "
        import json, sys
        data = json.load(sys.stdin)
        for m in data.get('manifests', []):
            arch = m['platform']['architecture']
            size = m.get('size', 0)
            print(f'{arch}: manifest size {size} bytes')
        "
        ```
        Cada plataforma debe ser ≤25 MB compressed (la imagen amd64 en local de Plan 03-01 ya estaba ≤25 MB; arm64 puede variar ligeramente).

    12. Smoke test funcional con la imagen pulleada de GHCR (no la local):
        ```bash
        docker rmi descriptor-cifrado:dev 2>/dev/null || true
        docker run -d --rm --name bed-ghcr-test -p 18080:8080 ghcr.io/semillabitcoin/descriptor-cifrado:latest
        sleep 3
        curl -sf http://127.0.0.1:18080/ | head -5
        docker stop bed-ghcr-test
        ```
        Debe retornar HTML.

    **Resultado esperado:**
    - Todos los success criteria del ROADMAP §Phase 3 verificables:
      1. ✓ pull sin credenciales funciona
      2. ✓ docker inspect muestra amd64 + arm64
      3. ✓ ldd-check verde + tamaño ≤25 MB

    Esto cumple `feedback_test_before_push.md` (probar imagen real, no solo build local).
  </how-to-verify>
  <resume-signal>
    Type "approved" si todos los pasos 1-12 pasaron. Anotar:
    - ¿`make-public` step PASS o FAIL? (informa SUMMARY sobre confiabilidad del endpoint /orgs/...)
    - ¿Se requirió toggle manual en GHCR UI? (sí/no)
    - Tamaño exacto de cada arch en GHCR
    - Tiempo total del workflow (cold start build-and-push)

    Si algo falla, describe (ej. "ldd-check fail con libssl encontrado", "build arm64 timeout 40 min", "GHCR pull denied tras toggle manual").
  </resume-signal>
  <action>
    CHECKPOINT MANUAL — el operador humano ejecuta los pasos descritos en `<how-to-verify>` arriba y reporta resultado al executor con la señal `<resume-signal>`.

    Pasos clave (resumen — ver `<how-to-verify>` para detalle completo):

    PRE-FLIGHT:
    1. `gh repo view semillabitcoin/descriptor-cifrado --json visibility` — confirmar PUBLIC (clave para herencia automática PKG-04)
    2. `git log -1 --format='%ae'` — confirmar email noreply `55397917+4rkad@users.noreply.github.com`

    PUSH:
    3. `git add Cargo.toml Dockerfile .dockerignore .github/workflows/docker.yml .planning/phases/03-docker-ghcr/`
    4. `git commit -m "feat(phase-3): docker multi-stage + GHCR multi-arch workflow"`
    5. `git push origin main`

    OBSERVAR Y VALIDAR:
    6. `gh run watch` — confirmar 3 jobs: build-and-push verde, ldd-check verde, make-public PASS o WARNING
    7. `docker buildx imagetools inspect ghcr.io/semillabitcoin/descriptor-cifrado:latest` — debe listar `linux/amd64` Y `linux/arm64` (PKG-02)
    8. `docker logout ghcr.io && docker pull ghcr.io/semillabitcoin/descriptor-cifrado:latest` — debe funcionar sin auth (PKG-04)
    9. Si pull denied: toggle manual en `https://github.com/orgs/semillabitcoin/packages/container/descriptor-cifrado/settings`, reintentar pull
    10. Smoke test pulled image: `docker run -d --rm -p 18080:8080 ghcr.io/semillabitcoin/descriptor-cifrado:latest && sleep 3 && curl -sf http://127.0.0.1:18080/`

    Acción del executor: tras recibir "approved" + datos del SUMMARY (tiempos, tamaños, ¿make-public funcionó vía API o manual?), escribir 03-02-SUMMARY.md y declarar Phase 3 completa.
  </action>
  <verify>
    <automated>test -f .github/workflows/docker.yml &amp;&amp; python3 -c "import yaml; yaml.safe_load(open('.github/workflows/docker.yml'))" &amp;&amp; gh run list --workflow=docker.yml --limit=1 --json status,conclusion 2>/dev/null | python3 -c "import sys, json; data = json.load(sys.stdin); sys.exit(0 if data and data[0].get('conclusion') == 'success' else 1)" || echo "Note: gh run list check requires workflow to have completed at least once on main"</automated>
  </verify>
  <acceptance_criteria>
    - El workflow `docker.yml` se disparó tras push a main (verificable con `gh run list --workflow=docker.yml`)
    - Job `build-and-push` conclusion = `success`
    - Job `ldd-check` conclusion = `success` (PKG-03 — sin libssl/libcrypto/etc.)
    - Job `make-public` conclusion = `success` o `failure` con `continue-on-error: true` (anotar cuál en SUMMARY para PKG-04)
    - `docker buildx imagetools inspect ghcr.io/semillabitcoin/descriptor-cifrado:latest` reporta entries para `linux/amd64` Y `linux/arm64` (PKG-02)
    - `docker pull ghcr.io/semillabitcoin/descriptor-cifrado:latest` desde shell sin `docker login` retorna éxito (PKG-04 — Success Criterion 1 ROADMAP)
    - Cada arch en GHCR ≤25 MB comprimida (PKG-01 — Success Criterion 3 ROADMAP)
    - `docker run` de la imagen pulleada de GHCR responde a `curl http://127.0.0.1:18080/` con HTML
    - El operador humano respondió "approved" al `<resume-signal>` con datos: tamaños por arch, tiempo total cold build, ¿make-public funcionó vía API o manual?
  </acceptance_criteria>
  <done>
    Workflow `docker.yml` ejecutado en CI tras push a main; imagen multi-arch publicada en GHCR; `docker pull` sin auth funciona; ldd-check verde. **PKG-01..04 todos cumplidos. Phase 3 completa.**
  </done>
</task>

</tasks>

<verification>
- [ ] `.github/workflows/docker.yml` válido (YAML parseable, 3 jobs)
- [ ] Versions actuales (qemu@v4, buildx@v4, login@v4, metadata@v6, build-push@v7)
- [ ] `platforms: linux/amd64,linux/arm64` en build-push (PKG-02)
- [ ] `ldd-check` job con grep + exit 1 (PKG-03)
- [ ] Triple mecanismo PKG-04: label image.source + make-public step (continue-on-error) + fallback manual documentado
- [ ] PR a main con estos cambios construye sin push (validación)
- [ ] Push a main publica `:latest` + `:sha-<7>` en GHCR multi-arch (PKG-02)
- [ ] `docker pull` desde máquina sin login funciona (PKG-04)
- [ ] `docker buildx imagetools inspect` reporta amd64 Y arm64 (Success Criterion 2)
- [ ] Imagen ≤25 MB comprimida por arch (Success Criterion 3 + PKG-01)
- [ ] `ldd-check` verde, sin libssl/libcrypto/native-tls (PKG-03 + SEC-03)
- [ ] `ci.yml` NO modificado (queda Rust-only, D-09)
</verification>

<success_criteria>
1. **Success Criterion 1 (ROADMAP):** `docker pull ghcr.io/semillabitcoin/descriptor-cifrado:latest` desde una máquina sin `docker login` retorna éxito (PKG-04).
2. **Success Criterion 2 (ROADMAP):** `docker buildx imagetools inspect ghcr.io/semillabitcoin/descriptor-cifrado:latest` lista entries para `linux/amd64` Y `linux/arm64` (PKG-02).
3. **Success Criterion 3 (ROADMAP):** Job CI `ldd-check` exit 0 sin libssl/libcrypto/native-tls; cada arch ≤25 MB comprimida (PKG-01 + PKG-03).
4. PRs corren build-only (no push); workflow no consume permisos `packages: write` en PRs.
5. Tags producidos correctamente: push a main → `latest` + `sha-<7>`; tag `v0.1.0` (futuro) → `0.1.0` + `0.1` + `0` + `latest`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-docker-ghcr/03-02-SUMMARY.md` con:
- ¿`make-public` step funcionó vía API o requirió toggle manual? (informa decisión futura sobre endpoint /orgs/...)
- Tiempo total del primer build (cold start, sin cache GHA)
- Tiempo del segundo build (con cache, esperado &lt;10 min)
- Tamaño exacto comprimido por arch (amd64 + arm64) reportado por GHCR manifest
- Confirmación de pull sin auth desde máquina limpia (paso 10 del checkpoint)
- Cualquier ajuste menor al workflow tras primer run (ej. timeout aumentado, cache key añadida, version bump de action)
- Si la herencia automática de visibilidad funcionó O fue necesario el step make-public O el toggle manual UI

**FASE 3 COMPLETA** tras este SUMMARY: PKG-01..04 todos cumplidos, imagen multi-arch pública en GHCR. Phase 4 (s9pk) puede consumir `ghcr.io/semillabitcoin/descriptor-cifrado:latest` sin auth.
</output>
