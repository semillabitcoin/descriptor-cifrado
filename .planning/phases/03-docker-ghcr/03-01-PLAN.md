---
phase: 03-docker-ghcr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Dockerfile
  - .dockerignore
autonomous: false
requirements: [PKG-01]
must_haves:
  truths:
    - "El binario release `bed-server` se reduce de tamaño tras añadir [profile.release] (strip + lto + panic=abort)"
    - "`docker buildx build --platform linux/amd64 .` compila la imagen completa sin errores"
    - "La imagen final usa base `gcr.io/distroless/cc-debian12:nonroot` (UID 65532)"
    - "La imagen comprimida pesa ≤25 MB"
    - "`docker run -p 8080:8080 <image>` arranca el servidor y la SPA carga en http://localhost:8080"
  artifacts:
    - path: "Cargo.toml"
      provides: "Sección [profile.release] en workspace root"
      contains: "[profile.release]"
    - path: "Dockerfile"
      provides: "Multi-stage build 3 stages: frontend-builder, rust-builder, runtime"
      contains: "FROM gcr.io/distroless/cc-debian12:nonroot"
    - path: ".dockerignore"
      provides: "Exclusiones de build context"
      contains: "target/"
  key_links:
    - from: "Dockerfile rust-builder stage"
      to: "frontend/dist/"
      via: "COPY --from=frontend-builder /app/frontend/dist /app/frontend/dist"
      pattern: "COPY --from=frontend-builder"
    - from: "Dockerfile runtime stage"
      to: "rust-builder /app/target/release/bed-server"
      via: "COPY --from=rust-builder"
      pattern: "COPY --from=rust-builder /app/target/release/bed-server"
    - from: "rust-embed (crates/server/src/assets.rs)"
      to: "/app/frontend/dist/ en build container"
      via: "#[folder = \"../../frontend/dist/\"] resuelve relativo a /app/crates/server/"
      pattern: "frontend/dist"
---

<objective>
Crear el Dockerfile multi-stage + .dockerignore + perfil de release optimizado en Cargo.toml para producir una imagen Docker `linux/amd64` distroless ≤25 MB que arranca `bed-server` y sirve la SPA embebida.

Purpose: Establecer la imagen base buildeable y verificable localmente antes de añadir multi-arch + CI push (Plan 03-02). Sin esto, el workflow CI no tiene Dockerfile que invocar.
Output: Cargo.toml con [profile.release], Dockerfile en raíz, .dockerignore en raíz, imagen `descriptor-cifrado:dev` buildeada localmente y verificada.

Cumple PKG-01 (multi-stage rust:slim → distroless/cc-debian12 ≤25 MB).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-docker-ghcr/03-CONTEXT.md
@.planning/phases/03-docker-ghcr/03-RESEARCH.md
@CLAUDE.md
@Cargo.toml
@frontend/package.json
@crates/server/src/assets.rs

<interfaces>
<!-- Layout del repo y rutas críticas que el Dockerfile debe respetar -->

Layout actual del repo (verificado):
```
descriptor-cifrado/
├── Cargo.toml              # workspace root, members = ["crates/core", "crates/server"]
├── Cargo.lock
├── rust-toolchain.toml     # channel = "stable"
├── deny.toml
├── crates/
│   ├── core/Cargo.toml
│   └── server/
│       ├── Cargo.toml
│       └── src/
│           ├── main.rs     # binario `bed-server`, bind 127.0.0.1:8080 (SEC-02)
│           └── assets.rs   # #[folder = "../../frontend/dist/"]
└── frontend/
    ├── package.json        # scripts.build = "vite build"
    ├── package-lock.json
    └── src/                # fuentes Svelte
```

rust-embed path resolution (de crates/server/src/assets.rs línea 19):
```rust
#[derive(RustEmbed)]
#[folder = "../../frontend/dist/"]
pub struct FrontendAssets;
```
Esta ruta es relativa al CARGO_MANIFEST_DIR del crate `bed-server` (que está en `/app/crates/server/` dentro del container). Por tanto rust-embed busca `/app/frontend/dist/` cuando WORKDIR=/app.

Workspace package metadata (Cargo.toml líneas 5-9):
```toml
[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"
authors = ["semillabitcoin"]
```

Frontend build output (verificado en Phase 2 SUMMARY): vite produce `frontend/dist/index.html` + `frontend/dist/assets/*` (JS+CSS+woff2 hashados).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Añadir [profile.release] al workspace Cargo.toml</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml (raíz, para confirmar estructura actual sin [profile.release])
    - .planning/phases/03-docker-ghcr/03-RESEARCH.md (sección "Cargo Release Profile (D-08)")
    - .planning/phases/03-docker-ghcr/03-CONTEXT.md (D-08 locked decision)
  </read_first>
  <action>
    Añadir al final de `Cargo.toml` raíz (después de la sección `[workspace.dependencies]`) el siguiente bloque exacto, implementando la decisión D-08:

    ```toml

    [profile.release]
    strip = true
    lto = "thin"
    codegen-units = 1
    opt-level = 3
    panic = "abort"
    ```

    Razones (de RESEARCH.md):
    - `strip = true`: elimina debug symbols (~50% reducción)
    - `lto = "thin"`: link-time optimization, balance speed/size
    - `codegen-units = 1`: mejor optimización a costa de compile time
    - `opt-level = 3`: explícito por claridad (default ya)
    - `panic = "abort"`: ahorra ~100-200 KB del unwind machinery; consistente con workspace lints `unwrap_used = "deny"` / `expect_used = "deny"`

    NO modificar otras secciones del Cargo.toml. NO añadir `[profile.release.package.bed-server]` (Pitfall 5 de RESEARCH.md: debe ser `[profile.release]` global del workspace).

    Tras el edit, ejecutar `cargo build --release --locked --bin bed-server` localmente para verificar que compila. El binario resultante debe ser ≤10 MB (esperado ~3-5 MB con strip vs 5.8 MB sin strip de Phase 2).
  </action>
  <verify>
    <automated>grep -E '^\[profile\.release\]' Cargo.toml &amp;&amp; grep -E '^strip = true' Cargo.toml &amp;&amp; grep -E '^panic = "abort"' Cargo.toml &amp;&amp; cargo build --release --locked --bin bed-server &amp;&amp; test $(stat -c%s target/release/bed-server) -lt 10485760</automated>
  </verify>
  <acceptance_criteria>
    - `grep '^\[profile\.release\]' Cargo.toml` devuelve match
    - `grep '^strip = true' Cargo.toml` devuelve match
    - `grep '^lto = "thin"' Cargo.toml` devuelve match
    - `grep '^codegen-units = 1' Cargo.toml` devuelve match
    - `grep '^opt-level = 3' Cargo.toml` devuelve match
    - `grep '^panic = "abort"' Cargo.toml` devuelve match
    - `cargo build --release --locked --bin bed-server` exit code 0
    - `stat -c%s target/release/bed-server` reporta valor &lt; 10485760 (10 MB)
    - `cargo test --workspace --locked` sigue verde (no se rompió Phase 1+2)
  </acceptance_criteria>
  <done>
    Cargo.toml contiene [profile.release] con los 5 settings exactos; el binario release native compila y pesa &lt;10 MB; los tests del workspace siguen pasando.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Crear .dockerignore en raíz</name>
  <files>.dockerignore</files>
  <read_first>
    - .planning/phases/03-docker-ghcr/03-CONTEXT.md (D-03 exclusiones)
    - .planning/phases/03-docker-ghcr/03-RESEARCH.md (sección ".dockerignore Raíz")
  </read_first>
  <action>
    Crear nuevo archivo `.dockerignore` en raíz del repo con el siguiente contenido EXACTO (implementa D-03):

    ```
    target/
    frontend/node_modules/
    frontend/dist/
    .planning/
    .git/
    .github/
    **/.DS_Store
    **/.env*
    **/.env
    *.md
    !LICENSE
    ```

    Razones:
    - `target/`: crate de Rust artifacts (~GB), se rebuilda en stage rust-builder
    - `frontend/node_modules/`: deps Node, se reinstalan via `npm ci` en frontend-builder
    - `frontend/dist/`: output de vite, se reconstruye en frontend-builder
    - `.planning/`: documentos de planificación, no necesarios en build
    - `.git/`: repo metadata
    - `.github/`: workflows CI (no necesarios para `docker build` local)
    - `**/.DS_Store`, `**/.env*`: ruido y secretos potenciales
    - `*.md` excepto `LICENSE`: README/docs no necesarios; LICENSE preservado por compliance

    NO añadir entradas adicionales (mantenerlo mínimo).
  </action>
  <verify>
    <automated>test -f .dockerignore &amp;&amp; grep -Fx 'target/' .dockerignore &amp;&amp; grep -Fx 'frontend/node_modules/' .dockerignore &amp;&amp; grep -Fx '!LICENSE' .dockerignore</automated>
  </verify>
  <acceptance_criteria>
    - `.dockerignore` existe en raíz
    - Contiene exactamente las 11 líneas listadas en `<action>` (sin extras)
    - `grep -Fx 'target/' .dockerignore` matches
    - `grep -Fx 'frontend/node_modules/' .dockerignore` matches
    - `grep -Fx 'frontend/dist/' .dockerignore` matches
    - `grep -Fx '.planning/' .dockerignore` matches
    - `grep -Fx '.git/' .dockerignore` matches
    - `grep -Fx '.github/' .dockerignore` matches
    - `grep -Fx '*.md' .dockerignore` matches
    - `grep -Fx '!LICENSE' .dockerignore` matches
  </acceptance_criteria>
  <done>
    `.dockerignore` creado en raíz con las 11 exclusiones exactas de D-03.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Crear Dockerfile multi-stage en raíz</name>
  <files>Dockerfile</files>
  <read_first>
    - .planning/phases/03-docker-ghcr/03-CONTEXT.md (D-01, D-13, D-14)
    - .planning/phases/03-docker-ghcr/03-RESEARCH.md (Pattern 1: Dockerfile 3-Stage Multi-Arch + Pitfalls 2 y 4)
    - frontend/package.json (confirmar scripts.build = "vite build")
    - crates/server/src/assets.rs (confirmar #[folder = "../../frontend/dist/"])
    - Cargo.toml (confirmar workspace members + Cargo.lock existe)
  </read_first>
  <action>
    Crear nuevo archivo `Dockerfile` en raíz del repo con el siguiente contenido EXACTO. Implementa D-01 (3 stages), D-13 (distroless cc-debian12:nonroot), D-14 (build args RUST_VERSION/NODE_VERSION):

    ```dockerfile
    # syntax=docker/dockerfile:1.7
    # BED Start9 — Bitcoin Encrypted Backup
    # Multi-stage build: frontend (Node) -> rust-builder (rust:slim-bookworm) -> distroless runtime

    ARG NODE_VERSION=20
    ARG RUST_VERSION=1

    # ── Stage 1: Frontend (Vite + Svelte 5) ─────────────────────────────────
    FROM node:${NODE_VERSION}-alpine AS frontend-builder
    WORKDIR /app/frontend
    COPY frontend/package.json frontend/package-lock.json ./
    RUN npm ci
    COPY frontend/ ./
    RUN npm run build
    # Output: /app/frontend/dist/

    # ── Stage 2: Rust workspace build ───────────────────────────────────────
    # Pin a -bookworm (Debian 12) para alineación con distroless/cc-debian12.
    # rust:1-slim apunta a Debian 13 (Trixie) desde Mar 2026 — incompatible glibc.
    FROM rust:${RUST_VERSION}-slim-bookworm AS rust-builder
    WORKDIR /app

    # 1. Workspace config (cambia poco, maximiza cache)
    COPY Cargo.toml Cargo.lock rust-toolchain.toml deny.toml ./

    # 2. Crate manifests (necesarios para que cargo resuelva el workspace)
    COPY crates/core/Cargo.toml crates/core/Cargo.toml
    COPY crates/server/Cargo.toml crates/server/Cargo.toml

    # 3. Frontend dist desde stage 1 — rust-embed lo lee en compile-time
    #    crates/server/src/assets.rs: #[folder = "../../frontend/dist/"]
    #    relativo a /app/crates/server/ → resuelve a /app/frontend/dist/
    COPY --from=frontend-builder /app/frontend/dist /app/frontend/dist

    # 4. Source code (cambia más frecuente, último para cache reuse)
    COPY crates/ crates/

    # --locked: respeta Cargo.lock exacto (CORE-01 pin v0.0.2)
    RUN cargo build --release --locked --bin bed-server

    # ── Stage 3: Distroless runtime ─────────────────────────────────────────
    # cc-debian12:nonroot incluye glibc + libgcc_s + libstdc++ mínimo.
    # UID/GID 65532 heredado del tag :nonroot. Sin shell, sin package manager.
    FROM gcr.io/distroless/cc-debian12:nonroot AS runtime
    COPY --from=rust-builder /app/target/release/bed-server /usr/local/bin/bed-server

    # Documentación; el bind real lo hace el binario en 127.0.0.1:8080 (SEC-02)
    EXPOSE 8080

    ENTRYPOINT ["/usr/local/bin/bed-server"]
    ```

    Notas críticas (de Pitfalls de RESEARCH.md):
    - **Pitfall 2:** `rust:1-slim-bookworm` (no `rust:1-slim`) por glibc compat con distroless/cc-debian12
    - **Pitfall 4:** `WORKDIR /app` + `COPY --from=frontend-builder /app/frontend/dist /app/frontend/dist` para que `#[folder = "../../frontend/dist/"]` resuelva correcto desde `/app/crates/server/`
    - **NO añadir `HEALTHCHECK`:** distroless no tiene shell; Phase 4 s9pk lo gestiona via SDK
    - **NO añadir `USER`:** el tag `:nonroot` ya configura UID 65532

    Tras crear el archivo, ejecutar build local amd64 para validar:
    ```bash
    docker build -t descriptor-cifrado:dev .
    docker image ls descriptor-cifrado:dev --format '{{.Size}}'
    ```
    Verificar que el build completa sin errores y la imagen pesa ≤25 MB descomprimida (el límite real comprimido es 25 MB; descomprimida en Docker local es ~30-40% más, pero como heurística rápida ≤30 MB local es ok).

    Para validar tamaño comprimido real (ver `<verify>`), usar `docker save | gzip | wc -c`.
  </action>
  <verify>
    <automated>test -f Dockerfile &amp;&amp; grep -F 'FROM gcr.io/distroless/cc-debian12:nonroot' Dockerfile &amp;&amp; grep -F 'rust:${RUST_VERSION}-slim-bookworm' Dockerfile &amp;&amp; grep -F 'COPY --from=frontend-builder /app/frontend/dist /app/frontend/dist' Dockerfile &amp;&amp; grep -F 'cargo build --release --locked --bin bed-server' Dockerfile &amp;&amp; grep -F 'ENTRYPOINT ["/usr/local/bin/bed-server"]' Dockerfile &amp;&amp; docker build -t descriptor-cifrado:dev . &amp;&amp; SIZE=$(docker save descriptor-cifrado:dev | gzip | wc -c) &amp;&amp; echo "Compressed size: $((SIZE/1024/1024)) MB" &amp;&amp; test "$SIZE" -lt 26214400</automated>
  </verify>
  <acceptance_criteria>
    - `Dockerfile` existe en raíz
    - `grep -F 'FROM gcr.io/distroless/cc-debian12:nonroot' Dockerfile` matches
    - `grep -F 'rust:${RUST_VERSION}-slim-bookworm' Dockerfile` matches (NOT plain `rust:1-slim`)
    - `grep -F 'FROM node:${NODE_VERSION}-alpine AS frontend-builder' Dockerfile` matches
    - `grep -F 'WORKDIR /app' Dockerfile` matches
    - `grep -F 'COPY --from=frontend-builder /app/frontend/dist /app/frontend/dist' Dockerfile` matches
    - `grep -F 'cargo build --release --locked --bin bed-server' Dockerfile` matches
    - `grep -F 'EXPOSE 8080' Dockerfile` matches
    - `grep -F 'ENTRYPOINT ["/usr/local/bin/bed-server"]' Dockerfile` matches
    - `docker build -t descriptor-cifrado:dev .` exit code 0 (no errores)
    - `docker save descriptor-cifrado:dev | gzip | wc -c` reporta valor &lt; 26214400 (25 MB comprimida — PKG-01)
    - `docker run --rm -d -p 18080:8080 descriptor-cifrado:dev` arranca el container; tras 3 segundos `curl -sf http://127.0.0.1:18080/` responde con HTML conteniendo "BED" o "<title>" (la SPA carga). Cleanup: `docker stop $(docker ps -q --filter ancestor=descriptor-cifrado:dev)`
  </acceptance_criteria>
  <done>
    Dockerfile multi-stage 3 stages crea imagen `descriptor-cifrado:dev` que pesa ≤25 MB comprimida, arranca el binario `bed-server` en distroless nonroot, y la SPA embebida responde en http://localhost:8080. PKG-01 cumplido para amd64 nativo.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verificación humana — round-trip cifrar/descifrar via container local</name>
  <files>(checkpoint — sin modificación de archivos; consume artefactos de Tasks 1-3)</files>
  <read_first>
    - Cargo.toml (verificar [profile.release] presente)
    - Dockerfile (verificar 3 stages)
    - .dockerignore (verificar exclusiones)
    - .planning/phases/03-docker-ghcr/03-RESEARCH.md (sección "Code Examples" + Pitfall 5 tamaño imagen)
  </read_first>
  <what-built>
    - Cargo.toml: añadido [profile.release] con strip+lto+codegen-units+opt-level+panic=abort
    - Dockerfile: 3 stages (frontend-builder Node 20 alpine → rust-builder rust:1-slim-bookworm → runtime gcr.io/distroless/cc-debian12:nonroot)
    - .dockerignore: 11 exclusiones de build context
    - Imagen local `descriptor-cifrado:dev` buildeada, ≤25 MB comprimida, arrancando `bed-server` en distroless nonroot

    Cumple PKG-01. Pendiente para 03-02: multi-arch (linux/amd64 + linux/arm64), GHCR push, ldd-check job, make-public.
  </what-built>
  <how-to-verify>
    Ejecutar el siguiente test end-to-end manual desde una shell en la raíz del repo:

    1. Asegurar que la imagen `descriptor-cifrado:dev` existe localmente:
       ```bash
       docker image ls descriptor-cifrado:dev
       ```
       Debe aparecer la entrada con un tamaño razonable.

    2. Verificar tamaño comprimido (PKG-01):
       ```bash
       docker save descriptor-cifrado:dev | gzip | wc -c
       ```
       El número debe ser &lt; 26214400 (≤25 MB). Anotar el valor obtenido.

    3. Arrancar container con volume para historial:
       ```bash
       mkdir -p /tmp/bed-data
       docker run -d --rm --name bed-test \
         -p 18080:8080 \
         -v /tmp/bed-data:/data/encrypted \
         descriptor-cifrado:dev
       sleep 2
       docker logs bed-test
       ```
       Debe ver "Listening on 127.0.0.1:8080" o similar.

    4. Cargar SPA en navegador:
       ```bash
       open http://localhost:18080  # o xdg-open en Linux
       ```
       Debe ver las dos pestañas "Cifrar" y "Descifrar" sin errores de consola, sin requests externos (verificar Network tab).

    5. Test funcional cifrar→descifrar con un descriptor multisig real (usa el descriptor de ejemplo de `crates/core/tests/`):
       - En tab Cifrar: pegar un descriptor con `<0;1>/*` y clicar "Cifrar". Verificar que aparecen 3 outputs (.bed download, armored copy, QR).
       - En tab Descifrar: pegar el armored del paso anterior + xpub válida del descriptor original. Verificar que se recupera el descriptor verbatim.

    6. Verificar que NO hay libssl en el binario dentro del container:
       ```bash
       docker run --rm --entrypoint="" descriptor-cifrado:dev /usr/local/bin/bed-server --help 2>&amp;1 | head -1 || true
       # NOTE: distroless no tiene shell ni ldd, por eso ldd-check job se ejecuta nativo en CI (Plan 03-02).
       ```

    7. Cleanup:
       ```bash
       docker stop bed-test
       rm -rf /tmp/bed-data
       ```

    **Resultado esperado:**
    - Tamaño comprimido ≤25 MB
    - Container arranca y bindea 8080
    - SPA carga sin requests externos
    - Round-trip cifrar→descifrar funciona en el container

    Esto cumple `feedback_test_before_push.md` (probar imagen real antes de push CI) para Phase 3.
  </how-to-verify>
  <resume-signal>
    Type "approved" si:
    - Tamaño comprimido reportado ≤25 MB
    - Container arrancó correctamente
    - SPA cargó sin errores ni requests externos
    - Round-trip cifrar→descifrar funcionó

    Si algo falla, describe el problema (ej. "tamaño 28 MB", "container muere con GLIBC error", "SPA no carga assets").
  </resume-signal>
  <action>
    CHECKPOINT MANUAL — el operador humano ejecuta los pasos descritos en `<how-to-verify>` arriba y reporta resultado al executor con la señal `<resume-signal>`.

    Pasos clave (resumen — ver `<how-to-verify>` para detalle completo):
    1. `docker image ls descriptor-cifrado:dev` — confirmar imagen existe localmente
    2. `docker save descriptor-cifrado:dev | gzip | wc -c` — confirmar &lt; 26214400 bytes (PKG-01)
    3. `docker run -d --rm --name bed-test -p 18080:8080 -v /tmp/bed-data:/data/encrypted descriptor-cifrado:dev`
    4. Cargar `http://localhost:18080` en navegador, verificar Network tab sin requests externos
    5. Round-trip cifrar→descifrar manual con descriptor multisig real (`<0;1>/*`)
    6. `docker stop bed-test; rm -rf /tmp/bed-data`

    Acción del executor: tras recibir "approved", continuar al siguiente plan (03-02).
  </action>
  <verify>
    <automated>docker image inspect descriptor-cifrado:dev > /dev/null 2>&amp;1 &amp;&amp; SIZE=$(docker save descriptor-cifrado:dev | gzip | wc -c) &amp;&amp; test "$SIZE" -lt 26214400 &amp;&amp; echo "Local image size: $((SIZE/1024/1024)) MB OK"</automated>
  </verify>
  <acceptance_criteria>
    - Imagen local `descriptor-cifrado:dev` existe (`docker image inspect descriptor-cifrado:dev` exit 0)
    - `docker save descriptor-cifrado:dev | gzip | wc -c` reporta valor &lt; 26214400 (PKG-01 confirmado)
    - El operador humano confirmó que el container arrancó y `curl http://localhost:18080/` retornó HTML de la SPA
    - El operador humano confirmó que round-trip cifrar→descifrar funcionó dentro del container
    - El operador humano respondió "approved" al `<resume-signal>`
  </acceptance_criteria>
  <done>
    Operador humano confirmó que la imagen Docker local arranca, sirve la SPA sin requests externos, y el round-trip funcional cifrar→descifrar funciona contra el container. PKG-01 validado en imagen real (no solo build).
  </done>
</task>

</tasks>

<verification>
- [ ] `Cargo.toml` tiene `[profile.release]` con los 5 settings exactos (D-08)
- [ ] `cargo build --release --locked --bin bed-server` produce binario &lt;10 MB en target/release/
- [ ] `cargo test --workspace --locked` sigue verde (Phase 1+2 no rotas)
- [ ] `.dockerignore` existe en raíz con las 11 exclusiones de D-03
- [ ] `Dockerfile` existe en raíz con 3 stages (frontend-builder, rust-builder, runtime)
- [ ] Dockerfile usa `rust:${RUST_VERSION}-slim-bookworm` (NO `rust:1-slim`)
- [ ] Dockerfile usa `gcr.io/distroless/cc-debian12:nonroot` como runtime
- [ ] `docker build -t descriptor-cifrado:dev .` completa sin errores
- [ ] `docker save | gzip | wc -c` reporta ≤25 MB (PKG-01)
- [ ] `docker run -p 18080:8080 descriptor-cifrado:dev` arranca el binario
- [ ] `curl -sf http://localhost:18080/` retorna HTML de la SPA
- [ ] Round-trip manual cifrar→descifrar funciona en el container (checkpoint 4)
</verification>

<success_criteria>
1. `docker save descriptor-cifrado:dev | gzip | wc -c` &lt; 26214400 → **PKG-01 cumplido localmente**
2. `docker build .` produce imagen funcional con base distroless/cc-debian12:nonroot
3. Round-trip cifrar→descifrar funciona dentro del container (checkpoint 4 aprobado)
4. Workspace tests siguen pasando (`cargo test --workspace --locked`)
5. Binario release pesa &lt;10 MB tras strip+lto
</success_criteria>

<output>
After completion, create `.planning/phases/03-docker-ghcr/03-01-SUMMARY.md` con:
- Tamaño exacto del binario release tras strip+lto
- Tamaño exacto de la imagen comprimida (`docker save | gzip | wc -c`)
- Versión exacta de `rust:1-slim-bookworm` usada (output de `docker run --rm rust:1-slim-bookworm rustc --version`)
- Cualquier ajuste menor al COPY layer order tras la primera build
- Confirmación de que rust-embed encontró `/app/frontend/dist/` correctamente
</output>
